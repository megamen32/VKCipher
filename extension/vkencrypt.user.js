// ==UserScript==
// @name         VK P2P AES-GCM
// @namespace    local
// @version      5.3.2
// @description  P2P шифрование VK: seed-фраза, AES-GCM, словарный транспорт и сборка длинных сообщений
// @author       VKEncrypt
// @match        https://vk.com/*
// @match        https://m.vk.com/*
// @match        https://vk.ru/*
// @match        https://m.vk.ru/*
// @match        https://web.vk.me/*
// @match        https://m.web.vk.me/*
// @grant        GM_getValue
// @grant        GM_setValue
// @grant        GM_deleteValue
// @grant        GM_xmlhttpRequest
// @connect      vk.com
// @connect      m.vk.com
// @connect      vk.ru
// @connect      m.vk.ru
// @connect      *.vk.com
// @connect      *.vk.ru
// @connect      userapi.com
// @connect      *.userapi.com
// @connect      mycdn.me
// @connect      *.mycdn.me
// @run-at       document-idle
// @updateURL    https://raw.githubusercontent.com/megamen32/vkencrypt/master/extension/vkencrypt.user.js
// @downloadURL  https://raw.githubusercontent.com/megamen32/vkencrypt/master/extension/vkencrypt.user.js
// ==/UserScript==

(function () {
    'use strict';

    // ============================================================
    // VK P2P AES-GCM v5.3.2
    //
    // Что умеет:
    // - НЕ показывает модалку сразу после установки.
    // - Пока ключей нет, кнопки возле поля ввода открывают настройку.
    // - В seed-модалках есть "глаз" для просмотра вводимой фразы.
    // - Из seed-фразы детерминированно генерирует k1..k4.
    // - Сохраняет НЕ seed-фразу, а только производные ключи.
    // - Поддерживает пользовательские ключи 64 hex.
    // - Поддерживает временный ключ только в памяти.
    // - Умеет автошифровать при клике отправки и при Enter.
    // - Shift+Enter оставляет как перенос строки.
    // - При включённом автошифровании ручной замок скрывается.
    // - Опционально кодирует payload в emoji, кириллицу или русский словарь.
    // ============================================================

    const APP_NAME = 'VK P2P AES-GCM';
    const APP_VERSION = '5.3.2';

    const FORMAT_START = '𓁗';
    const FORMAT_MID = 'Ⰴ';
    const FORMAT_PAYLOAD = 'Ⱑ';
    const CODEC_MARKERS = {
        base64: '𐌁',
        emoji: '𐌄',
        cyrillic: '𐌓',
        words: 'слова'
    };

    const BASE64_ALPHABET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';

    // 64 emoji для замены Base64-символов.
    // Важно: режим emoji опциональный, Base64 надёжнее для копирования/пересылки.
    const EMOJI_ALPHABET = [
        '😀','😁','😂','🤣','😃','😄','😅','😆',
        '😉','😊','😋','😎','😍','😘','🥰','😗',
        '😙','😚','🙂','🤗','🤩','🤔','🤨','😐',
        '😑','😶','🙄','😏','😣','😥','😮','🤐',
        '😯','😪','😫','🥱','😴','😌','😛','😜',
        '😝','🤤','😒','😓','😔','😕','🙃','🤑',
        '😲','😡','🤬','😖','😞','😟','😤','😢',
        '😭','😦','😧','😨','😩','🤯','😬','😰'
    ];

    const EMOJI_PAD = '🟰';
    const CYRILLIC_ALPHABET = [
        'А','Б','В','Г','Д','Е','Ж','З',
        'И','Й','К','Л','М','Н','О','П',
        'Р','С','Т','У','Ф','Х','Ц','Ч',
        'Ш','Щ','Ъ','Ы','Ь','Э','Ю','Я',
        'а','б','в','г','д','е','ж','з',
        'и','й','к','л','м','н','о','п',
        'р','с','т','у','ф','х','ц','ч',
        'ш','щ','ъ','ы','ь','э','ю','я'
    ];

    const CIPHER_CODECS = {
        base64: { shortCode: CODEC_MARKERS.base64, label: 'Base64' },
        emoji: { shortCode: CODEC_MARKERS.emoji, label: 'Emoji' },
        cyrillic: { shortCode: CODEC_MARKERS.cyrillic, label: 'Русский алфавит' },
        words: { shortCode: CODEC_MARKERS.words, label: 'Русские слова (экспериментально)' }
    };

    const README_URL = 'https://github.com/megamen32/vkencrypt#readme';
    const INSTALL_URL = 'https://raw.githubusercontent.com/megamen32/vkencrypt/master/extension/vkencrypt.user.js';
    const CYBERCHEF_URL = 'https://gchq.github.io/CyberChef/';
    const ONE_TIME_NOTE_SERVICES = [
        'PrivateBin: https://privatebin.net/',
        'Onetime Secret: https://onetimesecret.com/',
        'Password Pusher: https://pwpush.com/'
    ];
    const MEDIA_CONTAINER_MAGIC = 'VKEM1';
    const MEDIA_CONTAINER_EXT = '.vke';
    const MEDIA_ENCRYPTED_MIME = 'application/octet-stream';

    const IV_LEN = 12;
    const TAG_LEN = 16;
    const WORDS_DICTIONARY_ID = 'ru-common-8192-v3';
    const WORDS_BITS = 13;
    const MAX_VK_UTF16_UNITS = 4000;
    const WORDS_GROUP_TTL_MS = 7 * 24 * 60 * 60 * 1000;
    const WORDS_GROUP_STORAGE_KEY = 'vk_p2p_word_groups_v1';

    const DEFAULT_KEY_SLOT = 'k1';

    const STORAGE_KEYS = {
        DERIVED_KEYS: 'vk_p2p_derived_keys_v1',
        CUSTOM_KEYS: 'vk_p2p_custom_keys_v1',
        SETTINGS: 'vk_p2p_settings_v1',
        CHAT_KEY_SLOTS: 'vk_p2p_chat_key_slots_v1'
    };

    const KDF_SALT = 'vk-p2p-aes-gcm-v1';
    const KDF_ITERATIONS = 250000;

    let DERIVED_KEYS = null;
    let CUSTOM_KEYS = {};
    let TEMP_KEY = null;
    let CHAT_KEY_SLOTS = {};

    let currentKeySlot = DEFAULT_KEY_SLOT;
    let currentChatContextId = '';

    let settings = {
        autoEncrypt: false,
        saveDerivedKeys: true,
        autoDecrypt: true,
        cipherCodec: 'emoji',
        encryptMediaUploads: true
    };

    let isAutoSending = false;
    let skipNextAutoEncrypt = false;
    let lastEncryptedAt = 0;
    let pendingWordMessages = [];
    let scanTimer = null;
    let mediaPreviewObserver = null;
    const MEDIA_DECRYPT_CACHE = new Map();
    const STORAGE_FALLBACK_PREFIX = 'vk-p2p-fallback:';
    const RUNTIME_PLATFORM = detectRuntimePlatform();
    const RU_WORDS_DICTIONARY_SHA256 = "ec79152a77cda3fd3017fdca984642e08a480c4b6f466a07a5f7f98b2f46ad52";
    const RU_WORDS_DICTIONARY = "а б в г д е ж з и й к л н о п р с т у ц ч ш ы ь э ю я аж ай ак ас бы во вы го да до ее её ет же за из ил ит их ка ко ла ли ль ля ми мм мы на не ни но ну нь об ой он оп ос от ох пк по ру св се см со тв то ты ть уж ук ум ус ха чё эй эх ага акт аль бал бас бег без бес боб бог бой бум буш бык быт вал ван ваш ввс век вес вид вне вой вон вот все всё вуз где ген гид год гул дар два для дом дон дуб дух душ дэн его еда еле еще ещё жан жар жир жкх жук зал зам зао зоя зря зуб ибо изд изо или иль имя иск как код кое кол ком кто лад лед лес лик луг лук луч люк маг май мак мар мед меж мех миг мид мир моё мой муж мэр мяч над наш неё нём нет низ нии нос нтв ный оао оба око оля она они оно ооо ось пан пар пес пик пир под пол пот при про рад раз рай рак рао рев рим род рок рон рот ряд сад сам сей сие сок сон сти сто стр суд суп сша сын сыр сэм так там тем тип ток том тот три тур тут тыл увы уже ухо фон фут хит ход хор чай час чат чей чек чем чём чин что шаг шар шеф шея шок шоу щит экс эра это яма авто адам алла алло алый атом баба база бакс балл банк баня безо бить близ боец бокс боль бомж борт босс брат бред бунт быть ваза валя вася ввод веба ведь веко вена вера верх весы весь вещь взор виза вика вино винт витя вица вкус влад вниз вода воин волк воля врач вряд всём вход выть галя гимн глаз гном гора град гриб гром груз гусь даёт даже даль дань дата дать дача двое двор дева дело день джим джип дима диск дитя доза долг доля дома дочь друг дуга дуть духи душа дыра дюйм егор едва езда если есть жаль жара жать жена женя жест жечь живо жить жрец жюри залп заря зато заяц звон звук зима знак зона зять иван игла игра идёт идея идти изба икра илья имхо инна иной ирак итак итог июль июнь кадр катя кафе каша кино кира ключ кожа коза коли коль комп кора корм коса кофе край кран крах крем круг куба куда купе курс куст леди лена лень лето лечь леша либо лига лида лиза лиса лист лить лифт лицо лишь лорд лужа лыжа люба люся майк майя мало мама мари марк март матч мать маша маяк меню мера метр миля мимо мина мини мисс мозг море мочь мощь мрак муха мыло мыть мышь мэри надо надя небо нерв нету нина нить ниша нога ноль нора нота ночь нрав нуль ныне ныть няня обед обои один окно оный опыт орел орех отец офис очаг очки папа пара паук пена перо петр петь петя печь пиво пить план плач плащ плен плод поза пока поле полк пора порт поэт прах пред приз пруд пуля путь пыль пьер пять ради рама ранг рано рейс река речь рига рита ритм роды рожа роль рост рота роща рука рыба сайт сало саня сара сбор сбыт свет свод свое своё свой себе себя сейф село семь семя сено сеть сила след слог слон слух смит снег соль соня сорт софт союз срок стаж стан стас стая стих стол стон стоп стул стыд суть сухо суша счет счёт сюда таки такт твой тело тема темп тень тест тетя течь теща тихо тоже толк тора торт тост трое трон труд туда туча тьфу тяга угол удар уезд ужин узор уйти упор урал уран урок уста утка утро уход учет фаза факс факт фара фига финн флаг фонд фото фунт хлеб холл хоть хотя храм хуан царь цвет цель цена цент цикл цирк чаша чего черт член чтоб чудо чуть чушь шанс шина шить шкаф шлем штат шуба щель эльф эрик этаж этап этот эфир юбка юмор юный явно ядро язык яйцо яков ярко ясно яхта ящик абзац автор агент адрес актер актив акция алекс алена алеша алиса аллея алмаз альфа ангел арена арест армия артем артур архив бабка багаж базар балет банка барак баран барон башка башня бедро белок белый белье берег биржа битва бланк блеск блюдо бокал более борец брать бремя бренд бровь броня брэнд брюки будда будка будто буква букет бухта былой бытие вагон вадим ванна вверх ввиду вдали вдвое вдова вдруг ведро верно весна вести весть ветвь ветер ветка вечер вздох взлет взнос взять видео видно вилка вилла вирус висок вихрь влага влево влечь внизу вновь вовсе возле войти волна волос время вроде всего вслед вслух всюду вчера въезд выбор вывод выезд вызов выйти вылет выход гараж гарри генри герой глина глупо гнать голос гонка гордо горка горло город гость грамм грант грань грипп гриша гроза грубо груда грудь грунт грязь густо давид давно далее дания даром дверь денис диван диета димка длина дождь дойти долго домик домой доска досуг доход дочка драка драма дрова дрожь дрянь дырка ежели ехать жадно жажда жанна ждать жених живой жизнь жилец жилой жилье жутко забор завет завод зайти закат закон залив залог замуж запах заряд затем заход зачем звать звено здесь земля зерно знамя знать игрок извне икона иметь имидж иначе иоанн искра испуг исток исход казна какой камин канал канон капля карта касса катер каюта кисть кишка класс клейн книга князь ковер когда койка комар конец копье корея кость костя котел кража кровь кроме крона круто крыса крыша кубик кубок кузов кукла кулак культ купец купол курсы кухня кучка лавка ладно лампа ласка легко лезть лидия линия лично ловко лодка ложка лучше лысый любой магия майка майор малый мамин марка маркс марья масса матка медик между менее мерка место месяц метод мешок минус мирно много модем может можно мозги молча монах морда мороз моряк мотив мужик музей мусор мысль мышка мышца мягко набор навык назад найти налог намек народ наряд настя наука нация недра нежно некий некто немец немой нести нечто низко никак никто нитка ницше ничто новый ножка номер норма носок нужда нужно нынче обида облик обман обмен образ обрыв обряд обувь обход общее общий объем обыск огонь одеть ожить океан около округ олень ольга опора опрос опять орать орган орлов осень особа особо особь остро отбор ответ отдел отель отзыв отказ отряд отсек отход охота очень очерк палец палка папка парад парус пасть пасха пауза пачка певец пение пепел перед песня песок петля петух печка пилот пират пирог плеер племя плечо плита плоть плохо плыть повар повод поезд поиск пойти показ полет полка полно попов порог порок после поток потом поход почва почка почта почти пошёл поэма право прага пресс приют проба проза прочь прямо птица пульс пульт пункт пусть путем пчела пятка пятно радио разве разом район рамка ранее расти рвать ребро редко режим резко рельс речка робот ровно ролик роман рсфср рубеж рубль рукав русло ручей ручка рыбак рыбка рывок рыжий рынок рычаг рюмка рядом садик салат салон самец самка самый санкт сахар сбоку сверх света свеча свист своём свыше связь сдать сдача сдвиг седло седой сезон секта семья серый сесть сетка сжать сзади сиять склад склон сколь скоро скука слабо слава слеза слово смело смесь сметь смочь смысл снейп снизу снова снять совет сорок сосед сосна сосуд сотня софья спать спеть спина спирт спорт спрос спуск сразу среда среди ссора стадо сталь старт стать ствол стена стенд степь стиль стихи столб столь страх струя судно суета сумка сумма супер сущий сфера схема сцена сырой сырье сытый табак тайга тайна тайно также таков такой такси талия танец тариф таять театр тезис текст темно тесно тесто тетка тираж титул тихий ткань товар тогда толпа томас тонна точка точно трава треск треть тропа туман тупик тупой туфля удача улица умело уметь умный упрек успех устав учеба учить ущерб фасад федор ферма физик фильм финал фирма фланг форма форум фотка фраза франк франц фрейд фронт фрукт фрэнк хакер хвост химия холод хохот худой целое целый центр чайка часто часть чашка червь через череп черта честь четко чехов чечня число чтобы чужой шапка шепот шесть шкала школа шляпа шоссе шрифт штаны штука штурм щенок экран этика южный юрист явный ягода якобы якорь яркий ясный август акцент алтарь альбом альянс анализ аналог англия андрей анкета апрель аптека аренда аромат аспект африка баланс балкон банкир барьер бегать бедный бежать бездна береза беречь беседа беслан библия бизнес благой близко богиня бодрый боевой болото больше борода борьба братец бревно будить бумага бурный бывало бывать бывший быстро бюджет важный валера валить ванная варить вблизи ввести вдвоем ведать вектор велеть венера верить вернее верный версия верста весело весить весьма вечный вешать взамен взгляд взойти видеть видимо винить вкусно власть влезть влиять вместе вместо внести внешне внутри внутрь внучка водить водный военно воздух возить войско вокзал вокруг вообще вопрос восемь восток впасть вперед вплоть вполне вправо впредь впрямь всегда вскоре встать всякий второй выборы выгода выдать выдача вынуть выпить выпуск высота высший гадать газета галина гвоздь генрих герман герцог гибель гибкий гигант гитара глагол глазок глоток гнездо голова голубь гордый горечь гормон горный горшок горько горячо градус график грубый грузин грузия группа грусть гудеть гулять давать давить давний далеко дальше данные данный дарить дважды дверца двести девица девять делать делить деньги дерево десант десять деталь дефект дешево деяние джеймс джинсы джонни джордж диалог дизайн диплом добрый добыча дожить доктор долгий должен должно долина доллар допрос дорога дорого досада дракон другие другое другой дружба дружно дружок думать дышать европа египет ездить ерунда жалкий жевать желать железа железо желтый женька жертва жестко жидкий жилище жирный житель журнал жуткий забить забота забыть завтра задать задача зажать зажечь залить замена заново занять заодно запись запрет запрос запуск засада защита заявка звезда звонок здание зелень земляк земной злость знание знаток значит значок золото зрелый зрение зубной иванов играть избить издали издать изъять имение именно импорт индеец индекс иногда ирония искать истина италия кабель кабина каблук кавказ каждый казань казать казино камень камера канада карман качать кивать кидать кинуть кипеть кирпич класть клиент климат кличка клятва книжка кнопка коготь кодекс колесо конный контур кончик корень король корона корпус космос краска кратко кредит крепко кресло кривая кривой кролик крошка кружка кружок крышка крючок ксения кстати купить купюра курица курорт куртка кушать ларина легкий легкое лежать лезвие лекция лениво леонид лететь летний летный лечить листва листок литься личной личный лишить ловить логика ложный лозунг локоть ломать лондон лопата лошадь лучший льгота любить любовь маневр манера марина мартин массаж массив мастер махать машина мебель медаль медный мелкий менять металл мешать минута мирчай миссис мнение многие многий многое могила модель модный модуль моисей мокрый момент монета мораль мостик мощный мрачно мудрец мудрый музыка мундир мучить мягкий набить наверх наглый надеть надзор нажать налево налить налицо нанять наружу наряду натура научно начало начать небось неделя нежный некого некуда нельзя немало нервно нечего нибудь нижний низкий низший никита никуда ничего ничуть нищета ноготь носить ноябрь нужный обидно обилие облако обнять обойти оборот обречь община объект обычай обычно огород ограда огурец одежда одетый одеяло однако окошко оксана оплата орбита оргазм орудие осетия осмотр основа особый остров отбить отдать отдача отклик откуда отмена отныне отнюдь отойти отпуск отсюда оттого оттуда отчего отъезд офицер охотно оценка ошибка падать палата пальма пальто память панель парень пароль партия певица пейзаж первое первый передо переть период петров пехота печаль печень пешком пещера пиджак пионер писать письмо питать плавно плакат платеж платок пленка плотно плохой победа побить побыть поверх погоня подать подача подбор подвал поднос подряд подход подчас подъем поесть пожить позади поздно поклон покров полина полный полоса польза польша поляна помеха помочь помощь понять попить порода посему посуда потеря потому похоже почему почерк поэзия правда правый предел предок прежде премия прибор приезд призыв прийти приказ пример приток притом прицел причем причём пришёл пробка провод проект пройти пророк прорыв просто против проход прочий прочно птичий птичка пугать пузырь пускай пустой пустяк пылать пышный пятеро работа равный развод разгар раздел размах размер разный разрыв разряд ранить ранний расход расчет реалия ребята регион реестр резать резкий резюме рекорд ректор ремень ремонт рецепт речной решать решить ричард ровный роддом родина родной родные ростов рубаха рубить руслан ручной рыбный рыдать рыцарь рюкзак сажать самара сборка свежий сверху свести светло свитер связка святой сделка сейчас сектор секция сервер сервис сергей сердце серега сериал сигнал сидеть силуэт сильно синтез сияние скажем скамья сквозь скелет скидка скорбь скорее скорый скучно слабый слегка следом слепой словно словом сложно служба случай слышно смелый смешно снимок снятие собака совать совсем солнце сонный сосать состав спасти спектр сперва спинка список спичка сплошь способ спустя срочно стадия станок старик старый статуя статья стекло стивен стимул стихия стоить стойка столик сторож стоять страна стрела стресс строго струна стыдно сугубо судить судьба суметь сундук сунуть схожий счесть съемка съесть тайный талант тащить твердо творец телега темный теория теперь теплый теракт терять тесный техник ткнуть толком только тонкий тонуть тормоз тотчас точный трасса тренер третий трижды триста троица тройка трубка трудно тряпка трясти туалет туризм турнир турция тысяча тяжело тяжкий тянуть убогий уборка убрать убыток увезти увести увлечь угодно уголок угроза удачно удобно уехать ужасно узнать улыбка умение унести унитаз упадок упасть уплата упорно урадом урожай усилие услуга уснуть успеть устало устный утрата участь учение ученик ученый уютный фактор фермер фигура физика филиал фильтр фонарь фонтан формат футбол хирург ховать ходить хозяин хорошо хотеть хребет хрущев худший царить царица цвести цветок ценить цитата чайник чайный частый чердак черный чертеж честно четыре чистый читать чтение чубайс чудный шаблон шагать шедевр шерсть шестой шинель ширина широко шуметь шумный шутить эдуард экипаж эмоция этакий эффект юбилей юность ющенко являть январь ячейка абонент автобус активно алексей альберт амбиция андреев анекдот антенна аппарат аппетит асфальт атомный атрибут аукцион бабочка бабушка базовый барабан барышня бассейн батарея батюшка бегство бедняга бельгия бешеный бледный ближний близкие близкий близнец блокада блокнот богатый боковой болезнь больной больший большой ботинок бояться браться брачный бригада бросить будущее будущий бульвар бумажка бутылка быстрый бытовой валерий вариант василий вводить вежливо великий величие венгрия верблюд веревка вернуть верхний веселый веселье вестник ветеран ветерок взяться видение видимый витрина вкусный владеть владыка влажный влияние вложить внешний вносить воевать военный возврат возраст вольный вопреки воробей восторг восьмой впадать впервые вправду впрочем вручить всерьез вспышка встреча входить входной въехать выбрать вывезти вывеска вывести выгодно выехать вызвать выигрыш вылезти выпасть выплата вырасти вырвать выручка выслать высокий выучить выявить гагарин газовый галстук генерал георгий героиня гибнуть главное главный гладить гладкий гласить глубина глубоко глядеть глянуть годовой головка гораздо горький горячий господи готовый граница графика гребень греметь грозить грудной грустно грязный давайте дверной двигать девочка девушка девятый дедушка декабрь деление деревня держать дернуть дескать десятка десяток десятый детишки детский детство дешевый диагноз диаметр длинный длиться дневник дневной добавка доброта доверие догадка догнать договор доехать должный донести дорогой дорожка достать достичь древний дремать дрожать дружить дружный евгений емкость епископ жаждать желание желудок женский женщина жестоко забрать загадка загнать заговор задолго заехать закрыть закупка закуска заметка замужем замысел занавес занести занятие занятый заорать записка заранее заслуга заснуть застать застыть затрата заявить звенеть звонить звучать здешний здравый зеленый злиться злобный значить золотой зоопарк зрелище игровой игрушка избыток извлечь изгнать издание изделие изнутри изредка изрядно изучать изящный иметься импульс инвалид индивид инженер интерес иркутск испания истинно историк история кабинет кажется казарма казачий казнить капитан капуста картина карьера кассета каталог кафедра квадрат квартал кивнуть кислота клавиша классик книжный кожаный коллега колледж колодец колокол колония колонка колючий коляска команда комедия комитет комната комфорт конверт конечно контакт контора конфета кончить копейка корабль корзина коридор кормить коробка королев коротко котенок который кошелек крайний красиво красный красота краткий крейсер критика кровать крупный крутить крыльцо курение лавочка ласкать ласково лауреат лебедев легенда ледяной ленивый либерал линейка лишение лопнуть любимый лягушка магазин мальчик мамочка маршрут материя матрица махнуть машинка мелодия мемуары мерзкий мертвец мертвый мерцать местный механик мечтать миллион милость министр минутка мирдень мирзнак мирморе мировой мирокно мирсвет могучий молдова молодец молодой молоток молчать монитор монолог морской мужчина муравей мучение мчаться мыслить набрать наверно наверху навести награда надежда надежно надолго надпись наедине назвать наивный найтись наконец накрыть наличие намерен намного нанести направо нарочно настать наталья научить научный находка неважно невский негодяй недавно недаром недолго некогда нелепый неловко немалый немного нервный нередко нестись неудача неясный никакой никитин никогда николай новинка новичок новость ноутбук нулевой обедать обещать обидный обижать обитать область обложка обломок обморок обожать обочина образец обратно обрести обучать обучить общение обычный обязать одеться однажды ожидать окраска октябрь описать опытный оркестр освоить осенний особняк остаток острота отвезти отвлечь открыто открыть отличие отлично отметка отрезок отрывок отстать оттенок отчасти охотник оценить очередь ощутить ощущать падение палатка парашют пароход парочка партнер паспорт патриот паутина пахнуть педагог перевал перевод переезд перейти перелом перенос перерыв переход персона песенка писание пищевой плавать плакать планета платить платный плоский плотный площадь плюнуть плясать побрать повезти поворот повсюду подарок поднять подобие подобно подойти подруга подсчет подушка подъезд поездка поехать пожалуй пожилой поздний позиция познать покрыть полгода полдень полезно полезть ползать полигон политик полнота полоска полчаса помидор помнить понести понятие понятно попасть поперек поплыть пополам попытка порошок портить портрет порядок посадка поселок послать пособие посреди посылка потолок потомок похвала походка похожий поцелуй пошлина поэтому правило править правота предлог предмет прежний прибыть придать признак примета принтер принцип принять природа прирост приступ причина приятно пробить пробыть продажа продать продукт прожить пропуск просить простой простор просьба профиль процент процесс прочный прошлое прошлый прощать прятать публика пустить пустота пустыня пшеница пыльный пятерка пятьсот рабочий равнина радость разбить развить разгром разница раненый рассвет рассказ раствор расцвет рвануть реактор реакция реально ребёнка ребенок рейтинг реклама ремесло реплика ресница ресурсы реформа речевой решение рисунок родовой роковой романов роскошь рубрика русский рухнуть рыбалка рядовой самолет санкция сбегать сбивать сборная сборник свадьба светить светлый светчай свобода сводить связать сдавать сдаться сделать сегмент сегодня седьмой секунда селение семинар сенатор сердито серебро сержант сжимать сидение сиденье сильный синдром система сказать скакать скачать складка сколько скрипка скромно скрытый скучать скучный славный сладкий следить слишком слияние словарь сложный сломать служить слушать слыхать слышать слышный сменить смешать смирнов смутный смущать снаружи сначала снежный снижать снизить снимать сниться собачка соблазн собрать событие совесть создать сойтись соколов соленый сорвать сорокин соседка сотовый спартак спасать спасибо спонсор спорить спорный справка среднее средний срочный ставить стадион станция старина старший степень стереть стирать столько стонать сторона стоянка стрелка стрелок строгий строить стройка строчка студент ступать ступень стучать суббота суждено сумочка супруга суровый схватка сходный счастье считать сыграть сюрприз таковой таможня таракан тарелка твердый творить текущий телефон темнеть терапия терпеть тетрадь техника течение товарищ толстой толщина тоннель топливо торчать трактор трамвай тратить тревога трезвый трещина трибуна трогать тронуть тротуар трудный тусклый тяжелый тяжесть убегать убедить убежать убежище убирать уважать уверить уверять увидеть уволить угадать угодить угодный удалить ударный удаться удачный уделить уделять удобный указать украина улетать улететь уличный уложить умереть умерший умолять уносить упорный уровень уронить усадьба усвоить усилить условие условно успешно усталый уходить уцелеть участие участок учебник учебный училище учитель учиться фабрика фамилия феномен фигурка философ финансы фонарик формула француз функция хвалить хватать хватить хлопать хлопоты хозяйка холодно хороший храбрый хранить хроника хрупкий царство цветной целевой целиком ценовой цепочка церковь часовой частица частный человек чемодан четверо чеченец чешский чистить чистота чувство шекспир широкий шоколад штирлиц штурман экзамен экспорт элемент элитный энергия явиться явление яркость ярмарка ясность академик академия активный акционер аналогия ансамбль античный аргумент ахматова батальон бедность бедствие безумный беларусь бетонный биология блестеть близость божество болгария братство британия бродский булгаков бумажный быстрота валяться васильев введение вдобавок вежливый верность веровать вероятно вертолет верхушка весенний вечность вещество взаимный вздыхать взлететь взрослый видеться виктория винтовка включать включить владелец владение владимир властный влечение вложение внесение внимание водиться возвести возиться возможно возрасти волнение воробьев воровать воротник вплотную вращение временно всеобщий вскочить вставать вставить встроить вступить всячески выбежать выбирать выводить выделять выдержка выдумать выезжать вызывать выиграть выкинуть вылезать вылетать вылететь выложить вынимать выносить вынудить выпадать выпивать выписать выражать вырезать выставка вытащить вытекать вытереть вытирать выходить выходной выяснить выяснять газетный гарантия гармония гарнизон германия гимназия гипотеза глубокий говорить годиться головной горбачев господин гостиная государь готовить грамотно грузовик грузовой грустный грядущий губерния даваться давление давность двадцать движение дворянин девчонка дежурный действие демократ детектив диапазон дизайнер динамика директор добавить добиться добродом доброчай добывать доверить доверять доводить довольно догонять доказать доктрина документ доложить домашний достойно доходить дрогнуть думаться духовный евросоюз ежегодно желаемый желанный желающий железный жениться жестокий живопись животное жидкость жилищный забавный забегать забирать заболеть забывать завалить заведомо заверить заветный зависеть завязать загрузка заданный задержка задумать заказчик законный закрытие закрытый заливать заменить заменять замереть заметить заметный замечать замирать занимать западный запасной запереть запирать записать заповедь запросто застрять засыпать затянуть заходить захотеть защитить защитник защитный звездный звуковой звучание здоровый здоровье зловещий знакомая знакомый значение значимый ивановна идеально иерархия избавить избежать известно извинить изволить издавать издалека издатель издержка издержки излагать изложить изменить изменять изобилие изучение институт интервал интернет интерьер интуиция инцидент ирландия истерика истинный источник исходить исходный исчезать кадровый казаться казенный каменный каникулы карандаш картинка карточка картошка касаться кастрюля катиться качаться качество квартира километр классика классный коалиция коллегия командир комбинат комиссар комиссия компания комплекс комплект контракт контроль конфликт коренной короткий красивый крещение крикнуть кристалл критерий кровавый крымский кузнецов культура купаться кухонный лабиринт лампочка ласковый легкость лестница летопись лечебный лечиться линейный листовка лицензия личность лишенный логичный любовный максимум малейший марксизм массовый материал медицина медленно мелькать менеджер меняться мерседес метаться метафора методика механизм механика миграция микрофон миллиард миновать минувший мирбуква миркнига мирмысль мирпесня мирслово мирсмысл миртекст мирфраза младенец молекула молочный молчание морально мотоцикл мудрость мужество мысленно мышление набирать наверное наводить навсегда надевать надежный наделить нажимать название наземный называть наиболее наказать накопить наладить наливать намекать наметить наносить наоборот написать напиться напрасно например напротив напрямую напугать народный наружный насквозь наследие насмешка натянуть находить нацелить начаться начинать небрежно неважный неверный невинный невольно недавний недалеко нежность нелегкий немецкий немногие немногий немножко ненужный неполный неправда несмотря нетрудно неудобно нефтяной нехватка николаев новейший норвегия носитель носиться ночевать нынешний обвинить обещание обладать обнимать обновить обойтись оболочка обратить обращать обретать обстоять обучение обходить обширный общество общность объектив объявить овладеть оглядеть огромный одержать одинокий одиночка одобрить ожидание означать оказание окончить окружать окружить опасение оператор операция описание оплатить опоздать опросить оптимизм опускать опустить организм оригинал ориентир осветить освещать освоение ослабить основать основной особенно оставить остаться отбирать ответить ответный отвечать отводить отдавать отделить отделять отдыхать отказать открытие открытка открытый отличать отличить отложить отменить отметить отмечать отнимать относить отобрать отправка отравить отражать отразить отрезать отрывать отставка отходить отчаяние отчаянно отыскать официант оформить охватить охранник охранять очевидно очистить очнуться ощутимый ощущение павловна памятник памятный парадный парадокс параметр пассажир патриарх передать передача пережить перемена перепись пересечь перечень персонаж персонал перчатка песчаный петрович печально печатать печатный пирамида писатель писаться питаться плавание плановый побежать побочный поведать поверить повестка повлиять повысить повышать погасить погибать погибший погодить погубить погулять подавать подарить подвести поделать поделить подобный подписка подробно подружка подумать поединок пожалеть пожелать позабыть познание поиграть поискать поистине показать покачать покидать покойник покрытие покупать полагать полезный полететь политика половина положить получать получить полюбить полярный поменять помереть помещать помогать помощник поначалу понимать понятный поощрять поползти поправка попросту поражать поразить породить портфель поручить посадить посетить посещать посидеть поспешно поставка постоять поступок посылать потерять потянуть походить похороны почетный пошутить пояснить пояснять правовой праздник практика преграда предание преемник прелесть премьера препарат прервать прибытие привезти привести привычка придётся приемник приемный приехать призвать признать прийтись прикрыть прилавок примерно принести принятый присесть прислать присущий прихожая прическа пришелец приятный проблема проверка провести прогулка продавец продюсер проехать прозвище произвол пройтись просмотр проспект простить простота простыня протокол прохожий прочесть прощание прощение проявить прыгнуть публично пуговица пушистый пытаться работать работник радовать радостно разбитый разборка разведка развитие развитой развитый разговор раздумье различие разумный районный ракетный раковина рассудок рассылка растение реальный ревность редакция редкость режиссер репортаж рецензия решающий решиться римлянин ринуться рискнуть родитель родиться рождение рукопись рыночный садиться сбросить сведение сверкать свернуть светзнак светлана светморе световой светокно светполе светсвет светский светцвет сдвинуть сдержать северный сентябрь середина серьезно сигарета симпатия ситуация скамейка склонить склонный скорость скромный скрыться слабость служащий служение слушание смелость смеяться смотреть снабдить снижение собирать собрание советник согласие согласно сожалеть создание сознание солидный солнышко соловьев сомнение сообщать сообщить состоять социолог сочинить сочинять спасение спастись спокойно спросить спрятать спускать спустить средство старичок старость старушка старшина столовая страдать страница странный страшный стрельба строение стройный стукнуть судебный суждение существо схватить сходство сценарий съездить таблетка табличка твердить творение телесный тематика терпение теряться типичный тихонько толкнуть тонкость торговец торговля торговый точность традиция тридцать тропинка трудовой трястись туманный тянуться убеждать уважение уверенно увлечься удаление удивлять удобство указание украинец украсить украшать укрепить укрыться улучшить уместный упаковка упустить уракнига усесться усиление ускорить условный услышать успевать успешный уставать устареть устроить уступать уступить утешение уточнить утренний участник учащийся фантазия фондовый фотограф фрагмент хитрость хлопнуть холодный хотеться хохотать хранение художник цветовой целовать ценность цифровой частично чересчур четверть читатель читаться чудесный шведский шелковый шикарный школьный щелкнуть эволюция экология экономия являться ядовитый языковой абсолютно аварийный австралия адаптация азиатский аккуратно александр алкоголик андреевич андреевна атмосфера аудитория банальный бесплатно бизнесмен благодаря благодать блаженный ближайший блондинка богатство болтаться бронзовый бросаться буквально буржуазия бутерброд бухгалтер бюллетень важнейший вводиться ведомость ведомство велосипед вернуться вероятный вертеться вертикаль верховный вечеринка взглянуть видимость виднеться включение влюбиться вмешаться внедрение внезапный внешность внутренне возбудить воздушный возложить возможный возникать возражать возразить волновать волшебный вооружить воплотить ворваться воспитать восточный восходить вражеский вращаться врезаться временный всевышний вселенная всемирный вспомнить вспыхнуть вставлять встретить встречать встречный всяческий вторжение вцепиться вчерашний выбраться выбросить выглядеть выглянуть выдвигать выдвинуть выделение выдержать выключить выполнить выполнять выпускать выпускник выпустить выработка выражение вырастать вырастить вырваться высказать выскочить выслушать выставить выстроить выступать выступить выявление выяснение галактика генератор география глубинный голландия гордиться городской госпиталь гражданин греческий громадный двадцатый двигаться двинуться девяносто делегация держаться детальный диктатура диктовать дискотека дискуссия добавлять добродень доброполе довестись довольный дождаться донестись дополнить дополнять допустить доставать доставить достаться достигать достойный достояние древность дружеский евангелие еврейский ежегодный ежедневно екатерина елизавета журналист забраться забросить заведение завернуть завершать заглянуть заголовка заголовок задумчиво заключать заключить закончить закричать закрывать закрыться замедлить замечание замкнутый замолчать занавеска занятость заплакать заплатить заполнить заполнять запомнить запускать запустить заработок заседание заслужить заставить затронуть заявление земельный идеальный иерусалим избранный известный извинение извлекать изложение изменение изобрести изумление именовать импортный имущество индийский индикатор индустрия инспектор инспекция инстанция интеллект интересно интерфейс интонация искажение исключать искренний искусство испортить исправить испуганно испытание исчезнуть исчерпать итальянец казанский казахский казахстан картофель категория килограмм кинотеатр кирпичный классовый колебание коллектив коллекция командный коммунист компьютер конкретно контейнер континент концепция кончаться кормление коробочка коррекция косвенный косметика космонавт коснуться крайность красавица крохотный крошечный кружиться крутиться культурно латинский лекарство литератор литовский локальный лошадиный маленький мальчишка маргарита массивный математик мгновение мгновенно мгновенье медитация медленный медсестра мелькнуть мироблако мирстрока множество мобильник мобильный молодость монастырь монополия мороженое мотивация мраморный мыслитель наблюдать набраться навестить навстречу наглядный наградить надеяться надлежать назначать назначить наилучший накормить налоговый намерение написание наплевать наполнить наполнять направить нарастать нарушение насекомое население насколько наследник наставник настолько настоящее настоящий настроить настройка наступать наступить научиться начальник небольшой неведомый невысокий недоверие нежелание незадолго незаметно неизбежно неизменно некоторый ненадолго необычный непонятно неприятно непростой несколько несчастье неудачный неудобный нисколько ничтожный новенький номинация нормально нравиться обижаться областной облегчить облигация обобщение обработка обращение обсуждать обучаться общежитие объявлять объяснить объяснять обыватель обыденный одинаково одобрение ожидаться озаботить оказаться оказывать окружение описывать оппозиция оправдать оранжевый осваивать освещение осмотреть осмыслить оснастить основание особенный осознание оставлять остальное остальной остановка отбросить отвергать отверстие отвлекать отвлечься отделение отдельный отдохнуть отечество отключить открывать открыться отнестись отношение отомстить отправить отпускать отпустить отрицание отставать отступить отцовский отчаянный отчетливо охотиться охотничий оценивать очередной очертание очутиться ошибаться ошибиться ошибочный ощущаться партийный пассивный первичный перевозка передовой переехать переписка перестать петербург питерский пластинка платформа племянник плоскость плотность побеждать побережье поведение повернуть повиснуть повторный повторять повышение погаснуть погладить поглядеть погрузить подбежать подбирать подвижный подводить подводный поддержка подземный подлежать подняться подобрать подождать подписать подражать подробный подросток подходить подчинить подъехать пожелание позволить позволять позвонить поисковый показание поколение покрывать покушение полностью положение полотенце получение поместить помещение помниться помолчать понемногу понимание пообещать попадание попасться поправить попросить поражение поручение посвятить поселение посещение поскольку последний послужить послушать поспешить поставить поставщик постоянно построить постройка поступать поступить постучать посчитать потенциал потерпеть потомство потратить потрясать почтенный появиться появление правильно пребывать превысить преданный предатель предельно предстать президент президиум презирать презрение прекрасно прибавить прибежать прибывать приводить привозить привыкать привычный привязать пригодный придавать придумать прижаться прижимать признание призывать прикрытие приложить применить применять принимать приносить принцесса приняться природный присвоить присылать приходить причинять пробежать пробовать провайдер проверить проверять проводить проводник провожать проволока программа продавать проделать продукция продумать проезжать проектный проживать проиграть произойти проклятый пролететь проникать прописать проповедь просидеть протекать противник противный профессия профессор проходить процедура процессор прочитать прочность прочтение прошедший прощаться проявлять прятаться пустынный пятьдесят равенство разбивать разбиться разбудить развалины развивать разводить раздавать раздаться разделить разделять различать различный разложить разобрать разойтись разрешать разрешить разрушить разыскать раскрытие расширить расширять ребеночек революция регулярно резервный резиновый резолюция результат рекламный репетиция репутация решимость рождаться рождество розничный роскошный сантиметр сближение свадебный свалиться сверстник светбуква светкнига светмысль светпесня светслово светсмысл светтекст светфраза свидетель свободный сводиться связанный связывать сдаваться сделаться секретарь семейство семенович семьдесят сергеевич сердечный сердиться серьезный системный сказаться сказочный скользить следовать следующий сливаться словесный сложиться сложность сломаться служебный случайный случаться случиться слушатель слушаться слышаться смениться смешанный смириться смысловой снабжение снижаться снизиться сниматься соблюдать собраться совершать совершить советский совещание совместно совпадать согласный содержать соединять сожаление создавать создатель сознавать сократить сокровище сообщение сорваться соседство составить составной состояние сотворить сотрудник сохранить сохранять социально сочетание сочинение спаситель спектакль специфика спокойный спортсмен способный сработать сравнение сравнимый сражаться ставиться станислав стараться старинный стоимость столичный сторонник страдание страничка страстный стратегия страховка страховой строитель строиться студентка ступенька сходиться счастливо считаться татарский твориться текстовый телевизор телеканал терпеливо тогдашний торговать тормозить торопливо тотальный транспорт требовать тревожить тронуться трудиться тщательно убедиться увеличить уверенный увидеться увлечение уговорить уголовный удалиться удаляться удивиться удивление удивленно указанный указатель указывать украшение укреплять улыбаться уменьшать уменьшить умеренный умолчание упереться упираться упоминать упомянуть уравнение уральский ураоблако усиливать усилиться ускорение успокоить усталость установка утвердить ухаживать учитывать факультет фальшивый федорович философия финляндия фирменный формально фундамент хозяйство хранилище хранитель храниться цепляться церемония церковный частенько частичный частность человечек чемпионат честность четвертый штурмовик экземпляр экономика экономист экономить энтузиазм югославия абсолютный автомашина автомобиль автономный адекватный аккуратный активность актуальный александра американец английский англичанин архитектор ассоциация балтийский банковский безобразие безопасный безусловно беседовать бесконечно бесплатный беспокоить беспорядок бессильный бессмертие библиотека блаженство больничный британский бюрократия васильевна величайший величество венедиктов веселиться вздрогнуть взорваться визуальный вкладывать включаться внутренний возбуждать возвратить возвращать возглавить возмещение возмущение возникнуть возражение возрастать возрастной воображать вообразить вооружение воплощение воспринять восприятие восхищение враждебный врожденный вскрикнуть вследствие вспоминать вступление выбираться выдаваться выдающийся выделяться выигрывать выполнение выработать выражаться выраженный выразиться выставлять выстрелить вычисление выясниться выясняться гениальный германский гигантский глобальный говориться голосовать господство готовиться грузинский дальнейшее дальнейший двенадцать декларация демократия деревянный джентльмен длительный добираться добробуква доброволец доброкнига доброслово добротекст доброфраза догадаться дожидаться дополнение допустимый доставлять достаточно достигнуть достижение египетский ежедневный желательно жестокость живописный жительство заведовать завершение завидовать завоевание завтрашний загадочный заговорить загореться задаваться задуматься заказывать заключение замерзнуть заниматься записывать заработать заработный засмеяться заставлять затянуться захотеться знакомство знаменитый значимость зрительный избиратель извиниться извиняться изготовить издеваться измениться изменяться изначально изображать изобразить инвестиция инженерный инициатива инструктор инструкция инструмент интересный информация исключение исправлять испугаться испытывать кавказский калифорния клавиатура колебаться количество комбинация комплимент композиция компромисс комсомолец конечность конкретный конкурсный координаты коричневый культурный ликвидация лирический литература личностный логический лондонский любоваться любопытный магический магнитофон мастерская мастерство масштабный маяковский милосердие миррадость митрополит михайлович михайловна могущество молодежный молчаливый мониторинг московский мурманский мучительно набережная навязывать надежность называться наибольший наказывать накопление напечатать наполовину напоминать направлять нарисовать населенный наследство настойчиво настроение наткнуться находиться нахождение начинаться начинающий невероятно невозможно негативный недостаток недоумение незаконный незаметный незнакомец незнакомый неизбежный неизменный немедленно немыслимый необходимо необычайно неожиданно неподалеку неподвижно непонятный непрерывно неприятель неприятный нервничать несомненно нетерпение неудобство николаевич николаевна новогодний нормальный обернуться обеспечить обладатель облегчение обнаружить обновление обозначить обосновать образовать обратиться обращаться обрушиться обусловить обходиться объединять объективно объявление оглядеться оглянуться одинаковый однозначно окружающие окружающий оперативно оплачивать оправдание определить определять оптический опускаться опуститься ориентация освободить оскорблять осложнение осмелиться основатель осознавать оставаться осторожный отвергнуть отвращение отдаваться отдаленный отзываться отказаться отказывать откинуться отклонение откровение откровенно отличаться отмечаться относиться отозваться оторваться отправлять отработать отражаться отразиться отрываться отстаивать отсутствие отчетность официально оформление охватывать патриотизм пенсионный перебирать переводить переводчик передавать переживать переменная переносить пересекать переходить переходный печататься поблизости повторение повышаться повышенный поговорить подбородок подвергать подготовка поддержать поделиться подключить подозрение подоконник подписание подсказать подставить подходящий подчинение пожаловать пожалуйста поздравить позитивный показаться показывать покраснеть покупатель полагаться полководец полномочие положенный полуостров получаться получиться помещаться пообщаться попадаться популярный попытаться поработать порадовать порядочный поселиться посередине посетитель посмеяться посмотреть посольство поставлять постановка постепенно постигнуть постоянный пострадать построение потихоньку потреблять потрясение потянуться похоронить поцеловать появляться правильный пребывание превратить превращать предвидеть предельный предлагать предложить предъявить предыдущий прекрасный прекратить прекращать прелестный преодолеть престижный претендент приблизить привилегия привлекать привыкнуть пригласить приглашать придворный приемлемый признавать признаться прикладной приложение применение примечание приобрести приоткрыть приподнять припомнить присниться пристально приступать приступить прихватить проведение провокация проглотить продвинуть продолжать проживание прозвучать произвести произнести промежуток промолчать проникнуть пропустить прорваться проследить простейший просторный протяжение прохладный процветать процентный прошептать проявиться проявление психология пушкинский пятнадцать равновесие радоваться разбросать развернуть развестись разглядеть разделение раздражать разместить размещение размышлять разработка разрешение разрушение разумеется распахнуть расписание рассказать расставить расстаться расстояние расстроить рассуждать рассчитать расширение реализация реальность регулярный респондент республика референдум решительно российский руководить санитарный сбрасывать светоблако светстрока светулыбка сдерживать секретарша семнадцать серебряный сертификат сильнейший складывать склониться склонность склоняться скончаться скрываться скульптура смотреться сновидение собеседник собираться соблюдение собственно совершение совершенно советовать совпадение содействие содержание содержимое соединение солдатский соображать сообразить сообщаться сообщество сооружение составлять состояться сохранение социальный социология сочетаться сочувствие специалист спецслужба справиться справочник спрашивать спрятаться спускаться спуститься сравнивать стабильный стартовать стеклянный стесняться стрелковый стремиться стремление схватиться счастливый сэкономить творческий телефонный территория технология толкование требование тренировка тринадцать троллейбус трудящийся тщательный убежденный увлекаться увольнение удержаться удерживать удивляться узбекистан украинский укрепление улыбнуться уменьшение уникальный упоминание употребить управление упражнение уставиться установить устойчивый устранение устроиться устройство утверждать фантастика физиономия физический финансовый фиолетовый формальный фотография футбольный химический хорошенько целоваться чрезмерный шампанское шевелиться шестьдесят экспертиза экспертный экспозиция электричка энергичный эскадрилья эффективно абстрактный авиационный австрийский агрессивный аккумулятор акционерный аналогичный архитектура ассортимент африканский банкротство безнадежный безработица безработный белорусский бесконечный бесполезный беспомощный бессмертный благородный блокировать большинство вдохновение великолепно вероятность взаимосвязь владивосток влиятельный вмешиваться внимательно возбуждение возвращение возвышаться возможность возмутиться возмущаться воображение вооруженный восемьдесят воскликнуть воскресенье воспитатель воспитывать восхищаться восхождение впечатление вспомниться встретиться встречаться выбрасывать выдерживать вынужденный выполняться высказаться высказывать выступление вытаскивать гвардейский героический голландский государство гражданский грандиозный графический действовать действующий департамент деревенский диагностика диссертация добровольно добродетель докладывать допускаться достаточный достигаться достоверный достоевский драгоценный европейский единственно естественно заблудиться заблуждение заболевание завершиться зависимость заглядывать загрязнение задержаться заканчивать заключаться закончиться закрываться заместитель запомниться заслуживать затрагивать затруднение захватывать значительно известность изображение изобретение израильский иллюстрация императрица иностранный интеллигент интенсивный исполнитель исправление исследовать исторически истребитель кандидатура капитальный качественно клинический командовать комментарий компенсация компетенция комплексный компьютерра конституция конструктор конструкция консультант контрольный конференция координация королевский королевство космический коэффициент кремлевский лаборатория легендарный либеральный максимально манипуляция материально материнский медицинский минеральный минимальный мистический многократно многолетний модификация молоденький моментально мучительный наблюдатель наклониться направление напряженный наслаждение наступление натуральный нахмуриться национализм невероятный невозможный невыносимый негодование недоступный нейтральный немедленный необходимый неподвижный непонимание неправильно непрерывный непривычный несомненный новосибирск нормативный обеспечение обнаружение обозначение оборудовать обоснование образование обслуживать обсуждаться общественно объединение объясняться обыкновенно обязательно одиннадцать одиночество однозначный оказываться окрестность октябрьский олимпийский оперировать описываться оправдывать определение определенно оптимальный оптимизация организатор организация освобождать осматривать особенность осуществить отвернуться отдельность откладывать откровенный открываться отправиться отступление официальный оцениваться параллельно партнерство перебраться перевернуть перекресток перемещение переработка пересечение переставать перехватить перечислить перечислять перспектива планировать пластиковый поверхность повреждение повториться повторяться поглядывать погружаться погрузиться подвергнуть подготовить поддаваться поддержание подключение подниматься подозревать подписаться подписывать подробность подсознание подтвердить подчеркнуть поздравлять познакомить поклониться поликлиника полноценный пользование понедельник понравиться попробовать попрощаться последовать последствие последующий послышаться посредством постепенный посторонний поступление потребитель потребность потребовать поэтический православие праздничный праздновать практически превращение преданность предисловие предложение предпосылка предпочесть предпринять предприятие предсказать представить предстоящий предъявлять презентация презерватив прекращение преобладать преодоление преподавать препятствие пресловутый приближение приветствие привлечение приводиться привязывать приглашение приговорить пригодиться придумывать приключение примитивный приобретать приписывать пристальный пристрастие приходиться пробираться провалиться проговорить программист программный продаваться продвижение продолжение проигрывать производить происходить проноситься проработать просвещение просмотреть просыпаться протягивать прохождение прочитывать публиковать равнодушный развалиться развиваться развлечение раздаваться раздражение различаться размахивать размещаться размышление разногласие разработать разрешаться разъяснение располагать расположить рассмеяться рассмотреть рассуждение рассыпаться растеряться расходиться расширяться реализовать родственный руководство руководящий светрадость связываться сегодняшний симпатичный сказываться следователь случайность собственник собственный совершаться совершенный современник современный согласиться согласовать соглашаться сознательно сократиться соотношение составление сохраниться сохраняться специальный спокойствие способность справедливо справляться становиться становление старательно столкнуться страхование структурный супермаркет супружеский тактический талантливый театральный телевидение традиционно требоваться треугольник тысячелетие убедительно увеличивать увеличиться уверенность уговаривать удивительно указываться уменьшаться уменьшиться университет упоминаться употреблять управляемый управляющий усиливаться усмехнуться успокаивать успокоиться устремиться утверждение участвовать учительница учитываться фактический фиксировать формировать фотоаппарат французский холодильник хронический целостность численность чувственный чувствовать швейцарский шестнадцать электроника электронный эротический эффективный авторитетный американский аплодисменты базироваться безопасность безразличный беременность беспокойство бесчисленный благополучие благополучно благословить божественный вглядываться великолепный вертикальный внимательный возвращаться восемнадцать воспоминание воспринимать востребовать впоследствии всевозможный высказывание ганапольский генетический глобализация гуманитарный демонстрация деятельность динамический длительность добровольный догадываться договориться документация долгожданный долгосрочный доминировать единственный естественный журналистика задумываться замечательно значительный игнорировать изготовление издательство использовать исследование исторический исчезновение католический качественный квалификация коллективный колоссальный командировка коммерческий коммуникация конкурентный консультация конфигурация концентрация крестьянский криминальный литературный максимальный материальный методический министерство модернизация наименование намереваться направляться наслаждаться национальный недвижимость неинтересный ненормальный неоднократно неповторимый неправильный нравственный обеспечивать обмениваться обнаруживать обнаружиться оборудование обрабатывать обрадоваться образованный образоваться обследование обслуживание общественный объединиться обыкновенный оглядываться ограниченный ограничивать ограничиться одновременно ознакомиться операционный определенный определиться определяться опубликовать организовать органический оригинальный основательно основываться остановиться осторожность осуществлять отказываться откликнуться относительно параллельный передаваться передвижение перемещаться пересмотреть переспросить периодически планирование повиноваться поворачивать повседневный подвергаться поддерживать подполковник подсказывать пожаловаться пожертвовать позаботиться поздравление политический положительно пользователь пользоваться понадобиться популярность послевоенный посоветовать пострадавший правильность празднование практиковать превосходить превосходный превратиться превращаться предвыборный предлагаться предполагать предположить предпочитать предпочтение председатель предсказание представлять предчувствие прекратиться прекращаться преодолевать преподавание преследовать претендовать приближаться приблизиться приземлиться признаваться принадлежать приобретение приоритетный прислушаться приспособить пробормотать продвигаться продолжаться произведение производство происшествие пространство протестовать противоречие профессионал профилактика радиостанция развернуться разглядывать разнообразие разноцветный разочаровать расположение распоряжение рассказывать расслабиться рассмотрение расставаться рассчитывать растительный рациональный реабилитация регулировать рекомендация руководитель свердловский своеобразный свойственный совершенство совокупность согласование сознательный соответствие сопровождать составляющая сотрудничать справедливый сравнительно стабильность сталкиваться стремительно строительный существовать существующий сформировать теоретически терминология тестирование торжественно тоталитарный традиционный убедительный удивительный удовольствие укладываться употребление установление устойчивость устраиваться формирование формулировка человеческий человечество четырнадцать чрезвычайный эволюционный экзотический элементарный энциклопедия автоматизация автомобильный академический александрович александровна анализировать аналитический архангельский архитектурный бесконечность бессмысленный биологический благодарность благополучный благоприятный благословение всероссийский высказываться действительно дополнительно достоверность драматический задействовать задерживаться задолженность заканчиваться замечательный запланировать зафиксировать здравствовать землетрясение избирательный инновационный интеллигенция интенсивность интерпретация исключительно использование исследователь категорически классификация комсомольский конкурировать корпоративный лекарственный маркетинговый международный месторождение металлический мировоззрение моделирование муниципальный мусульманский невозможность недостаточный нежелательный необходимость неожиданность нижегородский новорожденный оборачиваться обязательство одновременный окончательный оправдываться ориентировать останавливать осуществление ответственный относительный отреагировать отсутствовать первоначально передвигаться периодический перспективный планироваться поблагодарить поверхностный повествование подвергнуться подразделение подразумевать подтверждение поздороваться поразительный последователь постановление постсоветский почувствовать правительство превосходство предательство предназначить предоставлять предотвратить предположение предпринимать представитель представиться представление предупреждать предусмотреть преподаватель преследование привязанность приготовиться приготовление прикосновение применительно принципиально провозгласить проголосовать прогрессивный производиться происхождение промежуточный проникновение просматривать противоречить публиковаться разговаривать разновидность разнообразный разочарование разрабатывать располагаться расположиться распорядиться распоряжаться рассматривать расспрашивать реализоваться революционный регулирование рекомендовать реконструкция русскоязычный свидетельство символический собственность современность содействовать сопровождение сопротивление сосредоточить сочувствовать специализация специальность специфический сравнительный средневековый стимулировать стихотворение стремительный строительство существование телевизионный теоретический торжественный трансформация тренироваться туристический увеличиваться удовлетворить удовлетворять удостоверение универсальный устанавливать установленный финансировать формироваться хозяйственный чувствоваться экономический экстремальный электрический электричество эффективность автоматический альтернативный благосостояние великобритания воздействовать вознаграждение воспитательный восприниматься воспроизводить вычислительный географический горизонтальный действительный договариваться договоренность доказательство документальный дополнительный заключительный закономерность иллюстрировать индустриальный интеллигентный интересоваться информационный инфраструктура исключительный использоваться количественный комментировать компенсировать консервативный констатировать конструктивный контролировать математический могущественный направленность национальность незначительный некоммерческий необыкновенный неопределенный несправедливый нравственность оборонительный обстоятельство общественность ограничиваться организованный организовывать осуществляться отвратительный очаровательный парадоксальный педагогический первоначальный поддерживаться предварительно предоставление предотвращение предполагаться предупреждение предшественник предшествовать преобразование препятствовать приблизительно приветствовать придерживаться принадлежность принципиальный прислушиваться присоединиться присоединяться приспособление присутствовать провинциальный продовольствие проектирование промышленность противоречивый противостояние путешественник путешествовать распространить распространять рождественский самостоятельно соответственно сопровождаться справедливость статистический сформироваться сформулировать управленческий фантастический функциональный цивилизованный электроэнергия энергетический ассоциироваться бессознательный взаимоотношение воспроизведение восстанавливать вспомогательный государственный демократический демонстрировать дипломатический железнодорожный законодательный налогообложение неблагоприятный образовательный останавливаться ответственность последовательно предоставляться предприниматель предусматривать преимущественно привлекательный продолжительный противодействие противоположный профессионализм профессионально разворачиваться распространение рекомендоваться самостоятельный символизировать соответствовать соответствующий соотечественник сосредоточиться социологический территориальный технологический университетский устанавливаться физиологический фундаментальный функционировать характеризовать административный действительность заинтересованный заинтересоваться интеллектуальный неопределенность непосредственный последовательный проанализировать программирование производственный пространственный профессиональный распространяться функционирование чувствительность благотворительный взаимодействовать главнокомандующий исследовательский квалифицированный правительственный представительство продолжительность прокомментировать противоположность руководствоваться самостоятельность свидетельствовать совершенствование характеризоваться зарегистрироваться несовершеннолетний последовательность продемонстрировать производительность предпринимательство сельскохозяйственный ф х ад ан ах га де дж ди ес рф ст ся хм хх цк юг яд ян аня бак бар бен бок бор ввп вич вор гад газ дед джо дно дым ева зад зло ира кгб кой кот кпк лак лев лоб мат мвд мгу миф мол ник нлп нож обо оон поп пух раб ран рог сан сми снг сэр тон топ ура хан цех шум эхо юля юра азия анна араб баку беда билл блин блог блок боря брак буря бюро ваня вина внук вова вред выше глеб гнев горе граф грек грех гроб губа дабы дама даша джек джин джон дума дура дядя евро елка жанр жопа злой змей змея иран кайф карл киев клан клуб коля конь кпрф кпсс крик крым куча лапа литр ложь луна макс марс марш мент миша мода мост мука мясо нато негр ниже овца окоп олег очко парк паша пища плюс пляж пост пояс рана раса риск роза руль русь саша сбой секс скот слой смех спид спор ссср стук танк таня тигр труп тьма ужас узел указ укол урод файл флот хаос хата холм хрен цепь часы штаб щека юкос юлия юрий аборт аллах антон банда белка билет благо божий борис бочка важно вдоль везде везти вечно взвод визит вклад водка вождь волга вопль врать гений голод голый давай девка демон дикий добро дэвид еврей елена жалко жарко живот жрать жуков заказ замок запад запас затея зверь злоба игорь идеал иисус ильич индия иосиф ирина иудей казак казнь каков китай козел копия кошка крест крыло кусок лгать левый лента лидер литва мадам майкл малыш мария маска масло месть метро мечта милый минск мишка мотор налет нефть нигде нищий нюанс обзор озеро опера опция орден отдых отчет павел пакет палач париж пацан перец пермь питер пламя плата побег пожар позор покой полюс поляк порыв посол прием принц пушка пытка пьеса пятый разум ружье сапог сарай сашка сбить сеанс север семен серия сжечь синий скала слева слуга слюна смена собор сойти строй судья сутки сухой съезд сынок сюжет тварь тепло топор тоска труба трусы турок уголь узкий фокус халат цифра чисто шахта шкура шофер шпион штраф шутка эпоха юноша авария артист бандит бензин берлин биться боевик болеть больно валюта вампир ведьма ветхий взятка видать видный виктор висеть володя ворона ворота вправе выбить выжить высоко глупый глухой гоголь гонять гореть греция громко грохот густой дворец дерьмо деться доклад доступ дурной дьявол ельцин есенин жадный жалеть жалоба жаркий задеть задний запеть захват звание зимний злодей измена кинжал кирилл клетка колдун колено колхоз коньяк корова костер костюм кошмар крайне кремль кризис критик крутой курить лагерь ладонь лариса латвия лесной летать летчик лишать лишний лунный максим мамаша маршал матрос мелочь мирдом мирный миссия мистер митинг михаил молния молоко монстр монтаж мутный насчет наташа нежели ночной огонек одесса опасно острый отнять отходы охрана палуба паника парить патрон пенсия петька печать платон платье погода подвиг подлый пожать ползти помимо портал привет приход провал проезд прочее прыжок путать пушкин пьяный раньше распад редкий ресурс ритуал роберт робкий родить рожать ругать сатана свинья северо секрет сережа сессия сестра сибирь символ сирота сказка скрыть снаряд снести справа ссылка ставка стакан старец статус стенка степан стража строка студия супруг тамара термин тишина толчок травма турист тюрьма убийца убитый урачай устать учесть ущелье фашизм фашист филипп хитрый хищник ценный чертов четкий чуждый швеция эпизод эшелон яблоко японец япония ярость авиация австрия автомат адвокат адмирал актриса америка апостол аркадий армения армянин беженец безумие безумно болтать бродить бросать бунтман ведение ведущий вершина вестись виталий включая вначале вовлечь вовремя водяной воронеж впереди вредный вручную всадник вскрыть вторник выгнать вылезть вынести выстрел гадость газпром галерея гвардия говорят голубой городок госдума господь госпожа грамота граната грозный громкий далекий дальний двойной действо деловой депутат дергать держава дефицит деятель дивизия дмитрий довести драться дыхание единица жалость жареный женатый жесткий завести зависть завтрак задание задница залезть заметно затылок здорово зеркало зритель избрать израиль изучить иллюзия империя инсулин интрига калитка капитал клиника колбаса колонна конкурс концерт кончать крепкий кричать круглый кусочек лечение ловушка людской масштаб матушка медведь меньший милиция минимум мирполе мирцвет младший молитва мрачный мужской напасть напиток невеста нелегко неплохо неужели обидеть оборона объятие одевать оказать окраина олигарх опасный осколок осудить отвести отнести отрасль палочка паровоз пациент пешеход питание плевать пленный повести повесть подпись позвать поймать покупка полевой полиция полночь половой полотно полтора посметь поспать постель предать премьер престиж престол прибыль прижать призрак прогноз протест прыгать психика пулемет пятница рабство ранение рваться религия решетка римский розовый рубашка сбежать свалить светдом сволочь сгореть сетевой силовой симптом скандал сложить собачий союзный спальня спешить спутник старуха столица странно страсть страшно субъект сумерки сходить тактика таскать татарин татьяна тележка темнота толкать толстый тренинг троцкий убивать удалять ударить удивить уезжать ужасный украсть умирать урадень уразнак ураморе ураокно ураполе урасвет урацвет устоять утонуть февраль финский франция христов христос царский цензура частота челюсть чемпион честный четверг шагнуть шапочка шахматы шептать эксперт эскадра эстония агрессия активист алгоритм алкоголь аналитик анатолий арабский аэродром аэропорт больница бороться важность валентин валютный величина верующий вечерний взорвать виновный внезапно водитель воинский вступать выгодный выдавать выделить выразить вытянуть вячеслав геннадий глупость голодный гордость григорий делаться делиться денежный дипломат дорожный дурацкий душевный европеец единство животный жизненно забивать заводить задавать зажигать заказать закурить заложить заложник заняться зарплата засунуть зачастую защищать заявлять иванович известие излишний измерять инвестор инстинкт интервью интимный инфекция инфляция искренне испугать испытать кампания кандидат кататься киевский кинуться кислород кладбище ключевой клясться коварный коктейль конгресс конечный контекст контраст королева красавец крепость крокодил лишиться ложиться любезный любитель любовник местечко молиться молодежь мощность музыкант мурманск мучиться нагрузка надоесть накануне наложить нападать напарник наполеон наркоман нарушать нарушить небесный неплохой нечистый новгород обвинять обезьяна обмануть обнажить обратный обсудить общаться огненный оппонент основное осознать осуждать отдельно отличный оторвать очевидец павлович партизан перебить передний переулок петровна площадка победить побывать повесить подавить пожарный покинуть покойный польский помешать попадать послание потрясти похитить почитать почтовый привлечь приговор принятие приятель прогресс прокурор пропасть профсоюз психиатр психолог развести раскрыть редактор ресторан решаться рисовать ругаться светдень свидание свободно свойство сельский семейный случайно смертный совпасть соперник соседний спиртное сплошной сравнить сражение стальной стандарт старание столетие стрелять сущность танковый товарный трагедия турецкий тюремный убийство угрожать узнавать унижение урабуква урамысль урапесня ураслово урасмысл уратекст урафраза утратить чиновник чудовище швырнуть школьник эмигрант эпидемия японский авторитет авторский агентство анастасия армейский армянский атаковать биография блестящий большевик борисович бормотать броситься валентина вашингтон вздохнуть виноватый волшебник восстание временной выживание выплатить горожанин гостиница грамотный групповой двигатель депрессия дистанция добраться доброморе должность допускать доступный единичный жизненный завершить завоевать задержать закрепить запретить запрещать захватить идеология излучение измерение император имперский исключить испанский исполнить исполнять китайский коммунизм компонент конвенция конкурент кончиться коррупция кредитный лейтенант лукашенко любовница любопытно магнитный маркетинг местность мирулыбка модератор моральный наверняка наказание нападение напомнить напрягать начальный невидимый некоторые ненависть нехороший нуждаться обвинение обидеться обитатель облегчать обязанный одеваться окончание олимпиада опасаться опасность опираться оскорбить осторожно отпечаток отражение отступать парижский парламент пенсионер перевести переворот перенести печальный побуждать повредить погибнуть поглощать подавлять подданный поддаться подлинный поднимать поклонник покончить полковник помчаться порождать посредник правитель правление превышать прерывать претензия прибегать приезжать приказать прилагать прилететь приличный приоритет причинить провинция проклятие пропадать пропорция протянуть публичный радостный разбирать разбойник разведчик разорвать разрушать револьвер репрессия рисковать светиться связаться священник священный секретный сергеевна сибирский скользкий следствие соединить солнечный социализм ссылаться стереотип стихийный судорожно танцевать тенденция торжество трактовка тревожный трудность убеждение уважаемый удаваться удариться улучшение управлять урастрока ураулыбка устранить уточнение федерация фестиваль физически чеченский шахматный швейцария экскурсия эмиграция этический адресовать алексеевич аппаратура арестовать артиллерия афганистан белоруссия беременный библейский буржуазный васильевич влюбленный воспитание вспыхивать готовность губернатор дисциплина добавление добиваться доказывать доноситься жаловаться заботиться задыхаться заклинание зарубежный избавиться избыточный иностранец интеграция исполнение капитализм катастрофа квадратный композитор константин корпорация крепостной крестьянин крупнейший либерализм математика менеджмент милиционер наблюдение надлежащий назначение напряжение настаивать настольный начальство независимо неизвестно некрасивый ненавидеть непременно несчастный обвиняемый обнаженный обозначать обстановка обсуждение объединить объяснение ограничить остановить отодвинуть офицерский переговоры письменный победитель подавление подхватить подъезжать показатель предстоять преступник преступный приказание прикрывать продолжить прозрачный пропаганда пропускать проснуться публикация равнодушно раскрывать славянский смертельно совместный соглашение сокращение специально спортивный статистика таможенный творчество телеграмма торопиться увеличение умственный уничтожать уничтожить урарадость устраивать учреждение фактически фашистский христианин цитировать чудовищный экспедиция энергетика этнический азербайджан алкогольный благодарить благодарный болезненный взволновать виртуальный возглавлять воздействие возрождение волноваться генеральный голосование гражданство григорьевич группировка десятилетие должностной достоинство заключенный здоровенный знакомиться исполниться итальянский командующий конкуренция критиковать критический любопытство меньшинство мероприятие милицейский музыкальный мусульманин наблюдаться направиться недовольный независимый неизвестный неожиданный объективный обязанность ограничение оперативный оскорбление переживание перестройка повернуться пограничный подмосковье подчиненный подчиняться полицейский постараться потребление предписание приготовить приказывать применяться приниматься присутствие проводиться произносить прокуратура пролетариат проститутка проявляться психический путешествие радикальный разбираться разобраться разработчик расстрелять реагировать регистрация религиозный родственник сексуальный содержаться создаваться сомневаться соображение сумасшедший существенно температура технический трагический уничтожение федеральный философский характерный хорошенький центральный цивилизация чрезвычайно эксперимент юридический альтернатива беспокоиться владимирович восстановить вспоминаться вырабатывать екатеринбург законопроект зарабатывать изобретатель интересовать классический командование коммунальный компьютерный механический недовольство недопустимый недостаточно обязательный окончательно опровергнуть освободиться освобождение отправляться отрицательно партизанский персональный подтверждать подчеркивать православный практический предоставить предупредить преимущество преступление преступность приватизация прикоснуться произвольный промышленный работодатель раскольников расстройство региональный следственный сомнительный соревнование столкновение студенческий субъективный существенный транспортный ходорковский христианский христианство эксплуатация эстетический автоматически администратор администрация бухгалтерский вмешательство возникновение гарантировать императорский искусственный корреспондент ликвидировать недоразумение независимость отечественный отрицательный парламентский петербургский познакомиться положительный потенциальный потребоваться президентский производитель противостоять распределение расследование романтический следовательно экологический эмоциональный артиллерийский бомбардировщик взаимодействие военнослужащий восстановление господствовать заинтересовать замешательство идеологический инвестиционный индивидуальный исполнительный многочисленный поворачиваться подозрительный правонарушение представляться самоуправление сопротивляться сотрудничество способствовать стратегический удовлетворение финансирование характеристика художественный чувствительный воспользоваться демографический здравоохранение конституционный непосредственно непредсказуемый организационный ориентироваться потребительский предварительный психологический рассматриваться республиканский законодательство зарегистрировать коммунистический несправедливость социалистический капиталистический привлекательность экспериментальный правоохранительный специализированный специализироваться предпринимательский".split(" ");
    // ============================================================
    // Platform
    // ============================================================

    function detectRuntimePlatform() {
        const ua = navigator.userAgent || '';
        const vendor = navigator.vendor || '';
        const host = location.hostname.toLowerCase();
        const isIOS = /iPad|iPhone|iPod/.test(ua) || (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);
        const isAndroid = /Android/.test(ua);
        const isSafari = /Safari\//.test(ua) &&
            !/Chrome|Chromium|CriOS|Edg|OPR|Firefox|FxiOS/.test(ua) &&
            /Apple/i.test(vendor || ua);
        const hasGMStorage = typeof GM_getValue === 'function' &&
            typeof GM_setValue === 'function' &&
            typeof GM_deleteValue === 'function';
        const hasGMNetwork = typeof GM_xmlhttpRequest === 'function';
        const siteFamily = host.endsWith('.vk.me') ? 'vk.me' : 'vk';

        return {
            ua,
            host,
            siteFamily,
            isIOS,
            isAndroid,
            isSafari,
            hasGMStorage,
            hasGMNetwork,
        };
    }

    function getPlatformDisplayName() {
        if (RUNTIME_PLATFORM.isSafari && RUNTIME_PLATFORM.isIOS) return 'Safari на iPhone/iPad';
        if (RUNTIME_PLATFORM.isSafari) return 'Safari';
        if (RUNTIME_PLATFORM.isAndroid) return 'Android';
        if (RUNTIME_PLATFORM.siteFamily === 'vk.me') return 'VK Me';
        return 'эта платформа';
    }

    function getCrossOriginMediaBlockReason(url) {
        if (!/^https?:/i.test(url || '')) return '';
        if (String(url).startsWith(location.origin)) return '';
        if (RUNTIME_PLATFORM.hasGMNetwork) return '';

        if (RUNTIME_PLATFORM.isSafari) {
            return `${getPlatformDisplayName()} не даёт расшифровать вложения на этом сайте`;
        }

        return 'Эта платформа не даёт расшифровать вложения на этом сайте';
    }

    function applyMediaPlatformBlock(box, reason) {
        if (!box) return;

        const meta = box.querySelector('.vk-p2p-media-meta');
        const error = box.querySelector('.vk-p2p-media-error');
        const decryptBtn = box.querySelector('.vk-p2p-media-btn');
        const downloadLink = box.querySelector('.vk-p2p-media-download');

        if (meta) {
            meta.textContent = reason;
        }
        if (error) {
            error.textContent = '';
        }
        if (downloadLink) {
            downloadLink.hidden = true;
            downloadLink.removeAttribute('href');
            downloadLink.removeAttribute('download');
        }
        if (decryptBtn) {
            decryptBtn.hidden = true;
            decryptBtn.disabled = false;
            decryptBtn.title = reason;
            decryptBtn.textContent = '🔓 Расшифровать вложение';
        }

        box.dataset.vkP2PPlatformBlocked = 'true';
    }

    // ============================================================
    // Storage
    // ============================================================

    function safeJsonParse(value, fallback) {
        try {
            if (!value) return fallback;
            return JSON.parse(value);
        } catch {
            return fallback;
        }
    }

    function canUseLocalStorage() {
        try {
            const probeKey = `${STORAGE_FALLBACK_PREFIX}probe`;
            localStorage.setItem(probeKey, '1');
            localStorage.removeItem(probeKey);
            return true;
        } catch {
            return false;
        }
    }

    function gmGetValueCompat(key, fallback) {
        if (typeof GM_getValue === 'function') {
            return GM_getValue(key, fallback);
        }

        if (!canUseLocalStorage()) {
            return fallback;
        }

        const value = localStorage.getItem(`${STORAGE_FALLBACK_PREFIX}${key}`);
        return value === null ? fallback : value;
    }

    function gmSetValueCompat(key, value) {
        if (typeof GM_setValue === 'function') {
            GM_setValue(key, value);
            return;
        }

        if (!canUseLocalStorage()) {
            return;
        }

        localStorage.setItem(`${STORAGE_FALLBACK_PREFIX}${key}`, value);
    }

    function gmDeleteValueCompat(key) {
        if (typeof GM_deleteValue === 'function') {
            GM_deleteValue(key);
            return;
        }

        if (!canUseLocalStorage()) {
            return;
        }

        localStorage.removeItem(`${STORAGE_FALLBACK_PREFIX}${key}`);
    }

    function gmGetJson(key, fallback) {
        return safeJsonParse(gmGetValueCompat(key, null), fallback);
    }

    function gmSetJson(key, value) {
        gmSetValueCompat(key, JSON.stringify(value));
    }

    function loadSettings() {
        const saved = gmGetJson(STORAGE_KEYS.SETTINGS, null);
        if (saved && typeof saved === 'object') {
            const normalized = { ...saved };

            if (typeof normalized.autoDecrypt !== 'boolean' && typeof normalized.decryptIncoming === 'boolean') {
                normalized.autoDecrypt = normalized.decryptIncoming;
            }

            if (!Object.prototype.hasOwnProperty.call(normalized, 'cipherCodec')) {
                normalized.cipherCodec = normalized.emojiCipher ? 'emoji' : 'base64';
            }

            settings = {
                ...settings,
                ...normalized
            };
        }
    }

    function saveSettings() {
        gmSetJson(STORAGE_KEYS.SETTINGS, {
            ...settings,
            decryptIncoming: settings.autoDecrypt,
            emojiCipher: settings.cipherCodec === 'emoji'
        });
    }

    function isValidKeyHex(hex) {
        return typeof hex === 'string' && /^[0-9a-f]{64}$/i.test(hex);
    }

    function areValidDerivedKeys(keys) {
        return Boolean(
            keys &&
            isValidKeyHex(keys.k1) &&
            isValidKeyHex(keys.k2) &&
            isValidKeyHex(keys.k3) &&
            isValidKeyHex(keys.k4)
        );
    }

    function normalizeKeyObject(obj) {
        const out = {};
        for (const [k, v] of Object.entries(obj || {})) {
            if (isValidKeyHex(v)) out[k] = String(v).toLowerCase();
        }
        return out;
    }

    function normalizeCustomKeyEntry(raw) {
        if (!raw) return null;

        if (typeof raw === 'string') {
            if (!isValidKeyHex(raw)) return null;
            return { key: raw.toLowerCase(), label: '' };
        }

        if (typeof raw === 'object') {
            if (!isValidKeyHex(raw.key)) return null;
            const label = typeof raw.label === 'string'
                ? raw.label.trim().slice(0, 64)
                : '';
            return { key: String(raw.key).toLowerCase(), label };
        }

        return null;
    }

    function loadDerivedKeys() {
        const saved = gmGetJson(STORAGE_KEYS.DERIVED_KEYS, null);
        if (areValidDerivedKeys(saved)) return normalizeKeyObject(saved);
        return null;
    }

    function saveDerivedKeys(keys) {
        if (!areValidDerivedKeys(keys)) return;
        gmSetJson(STORAGE_KEYS.DERIVED_KEYS, normalizeKeyObject(keys));
    }

    function clearDerivedKeys() {
        gmDeleteValueCompat(STORAGE_KEYS.DERIVED_KEYS);
        DERIVED_KEYS = null;
    }

    function loadCustomKeys() {
        const saved = gmGetJson(STORAGE_KEYS.CUSTOM_KEYS, {});
        const out = {};
        for (const [slot, raw] of Object.entries(saved || {})) {
            const normalized = normalizeCustomKeyEntry(raw);
            if (normalized) out[slot] = normalized;
        }
        CUSTOM_KEYS = out;
    }

    function saveCustomKeys() {
        gmSetJson(STORAGE_KEYS.CUSTOM_KEYS, CUSTOM_KEYS);
    }

    function resetAllKeys() {
        clearDerivedKeys();
        gmDeleteValueCompat(STORAGE_KEYS.CUSTOM_KEYS);
        clearChatKeySlots();
        CUSTOM_KEYS = {};
        TEMP_KEY = null;
        currentKeySlot = DEFAULT_KEY_SLOT;
        updateEncryptButtonsTitle();
        showSeedSetupModal();
    }
    // ============================================================
    // Per-chat key selection
    // ============================================================

    function normalizeChatKeySlots(value) {
        const normalized = {};

        for (const [chatId, slotId] of Object.entries(value || {})) {
            if (!/^vk:(peer|chat):[^\s]{1,96}$/.test(chatId)) continue;
            if (typeof slotId !== 'string' || !slotId || slotId.length > 64) continue;
            normalized[chatId] = slotId;
        }

        return normalized;
    }

    function loadChatKeySlots() {
        CHAT_KEY_SLOTS = normalizeChatKeySlots(
            gmGetJson(STORAGE_KEYS.CHAT_KEY_SLOTS, {})
        );
    }

    function saveChatKeySlots() {
        gmSetJson(STORAGE_KEYS.CHAT_KEY_SLOTS, CHAT_KEY_SLOTS);
    }

    function normalizeChatPeerId(rawValue) {
        let value;
        try {
            value = decodeURIComponent(String(rawValue || ''));
        } catch {
            return '';
        }

        value = value.trim().replace(/^peer:/i, '');
        if (!value || value.length > 96) return '';

        if (/^c\d+$/i.test(value)) {
            return `vk:chat:${value.toLowerCase()}`;
        }
        if (/^-?\d+$/.test(value)) {
            return `vk:peer:${value}`;
        }
        if (/^[a-z0-9_.-]+$/i.test(value)) {
            return `vk:peer:${value.toLowerCase()}`;
        }

        return '';
    }

    function getCurrentChatId(urlValue = location.href) {
        let url;
        try {
            url = new URL(urlValue, location.origin);
        } catch {
            return '';
        }

        const queryPeer = ['sel', 'peer', 'peer_id', 'chat', 'convo']
            .map(name => url.searchParams.get(name))
            .find(Boolean);
        if (queryPeer) return normalizeChatPeerId(queryPeer);

        const pathMatch = /\/(?:mail\/)?convo\/([^/?#]+)/i.exec(url.pathname) ||
            /\/im\/(?:convo\/)?([^/?#]+)/i.exec(url.pathname);
        return pathMatch ? normalizeChatPeerId(pathMatch[1]) : '';
    }

    function getFallbackKeySlot() {
        const allKeys = getAllKeys();
        if (allKeys[DEFAULT_KEY_SLOT]) return DEFAULT_KEY_SLOT;
        return Object.keys(allKeys)[0] || DEFAULT_KEY_SLOT;
    }

    function rememberKeyForCurrentChat(slotId) {
        const chatId = getCurrentChatId();
        if (!chatId) return false;

        CHAT_KEY_SLOTS[chatId] = slotId;
        currentChatContextId = chatId;
        saveChatKeySlots();
        return true;
    }

    function selectKeySlot(slotId, { rememberForChat = true } = {}) {
        if (!getAllKeys()[slotId]) return false;

        currentKeySlot = slotId;
        if (rememberForChat) rememberKeyForCurrentChat(slotId);
        updateEncryptButtonsTitle();
        return true;
    }

    function applyRememberedKeyForCurrentChat({ force = false } = {}) {
        const chatId = getCurrentChatId();
        if (!chatId) return false;
        if (!force && currentChatContextId === chatId) return false;

        currentChatContextId = chatId;
        const allKeys = getAllKeys();
        const rememberedSlot = CHAT_KEY_SLOTS[chatId];
        let nextSlot = rememberedSlot;

        if (!nextSlot || !allKeys[nextSlot]) {
            nextSlot = getFallbackKeySlot();
            if (rememberedSlot) {
                delete CHAT_KEY_SLOTS[chatId];
                saveChatKeySlots();
            }
        }

        if (currentKeySlot === nextSlot) return false;
        currentKeySlot = nextSlot;
        updateEncryptButtonsTitle();
        return true;
    }

    function forgetChatKeySlot(slotId) {
        let changed = false;

        for (const [chatId, savedSlot] of Object.entries(CHAT_KEY_SLOTS)) {
            if (savedSlot !== slotId) continue;
            delete CHAT_KEY_SLOTS[chatId];
            changed = true;
        }

        if (changed) saveChatKeySlots();
    }

    function clearChatKeySlots() {
        CHAT_KEY_SLOTS = {};
        currentChatContextId = '';
        gmDeleteValueCompat(STORAGE_KEYS.CHAT_KEY_SLOTS);
    }

    function initChatKeyNavigation() {
        if (history.pushState?.vkP2PWrapped) return;

        const refresh = () => {
            currentChatContextId = '';
            applyRememberedKeyForCurrentChat({ force: true });
            scheduleScan();
        };

        ['pushState', 'replaceState'].forEach(methodName => {
            const original = history[methodName];
            if (typeof original !== 'function') return;

            const wrapped = function (...args) {
                const result = original.apply(this, args);
                refresh();
                return result;
            };
            wrapped.vkP2PWrapped = true;
            history[methodName] = wrapped;
        });

        window.addEventListener('popstate', refresh);
    }
    // ============================================================
    // Crypto helpers
    // ============================================================

    function hexToBytes(hex) {
        if (!isValidKeyHex(hex)) throw new Error('Invalid key hex');
        const arr = new Uint8Array(hex.length / 2);
        for (let i = 0; i < hex.length; i += 2) {
            arr[i / 2] = parseInt(hex.substr(i, 2), 16);
        }
        return arr;
    }

    function bytesToHex(bytes) {
        return Array.from(bytes)
            .map(b => b.toString(16).padStart(2, '0'))
            .join('');
    }

    function bytesToBase64(bytes) {
        let binary = '';
        const chunkSize = 0x8000;

        for (let i = 0; i < bytes.length; i += chunkSize) {
            const chunk = bytes.subarray(i, i + chunkSize);
            binary += String.fromCharCode.apply(null, chunk);
        }

        return btoa(binary);
    }

    function utf8ToBytes(text) {
        return new TextEncoder().encode(text);
    }

    function bytesToUtf8(bytes) {
        return new TextDecoder().decode(bytes);
    }

    function concatBytes(parts) {
        const total = parts.reduce((sum, part) => sum + part.length, 0);
        const out = new Uint8Array(total);
        let offset = 0;

        parts.forEach(part => {
            out.set(part, offset);
            offset += part.length;
        });

        return out;
    }

    function uint32ToBytes(value) {
        const out = new Uint8Array(4);
        new DataView(out.buffer).setUint32(0, value, false);
        return out;
    }

    function bytesToUint32(bytes, offset = 0) {
        return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).getUint32(offset, false);
    }

    function base64ToBytes(b64) {
        const bin = atob(b64);
        const data = new Uint8Array(bin.length);

        for (let i = 0; i < bin.length; i++) {
            data[i] = bin.charCodeAt(i);
        }

        return data;
    }

    function encodeBase64ToAlphabet(b64, alphabet, padChar = '=') {
        let out = '';

        for (const ch of b64) {
            if (ch === '=') {
                continue;
            }

            const idx = BASE64_ALPHABET.indexOf(ch);
            if (idx === -1) throw new Error('Invalid base64 char: ' + ch);
            out += alphabet[idx];
        }

        return out;
    }

    function decodeAlphabetToBase64(payload, alphabet, padChar = '=') {
        let out = '';

        for (const symbol of Array.from(payload)) {
            if (symbol === padChar) {
                out += '=';
                continue;
            }

            const idx = alphabet.indexOf(symbol);
            if (idx === -1) throw new Error('Invalid cipher symbol: ' + symbol);
            out += BASE64_ALPHABET[idx];
        }

        return out + '='.repeat((4 - (out.length % 4)) % 4);
    }

    function encodeBase64ToEmoji(b64) {
        return encodeBase64ToAlphabet(b64, EMOJI_ALPHABET, EMOJI_PAD);
    }

    function decodeEmojiToBase64(payload) {
        return decodeAlphabetToBase64(payload, EMOJI_ALPHABET, EMOJI_PAD);
    }

    function encodeBase64ToCyrillic(b64) {
        return encodeBase64ToAlphabet(b64, CYRILLIC_ALPHABET);
    }

    function decodeCyrillicToBase64(payload) {
        return decodeAlphabetToBase64(payload, CYRILLIC_ALPHABET);
    }

    function getCipherCodecConfig(codecId) {
        return CIPHER_CODECS[codecId] || CIPHER_CODECS.emoji;
    }

    function normalizeCodecId(codecId) {
        return Object.prototype.hasOwnProperty.call(CIPHER_CODECS, codecId) ? codecId : 'emoji';
    }

    function encodePayloadForCodec(b64, codecId) {
        switch (normalizeCodecId(codecId)) {
            case 'base64':
                return b64.replace(/=+$/u, '');
            case 'cyrillic':
                return encodeBase64ToCyrillic(b64);
            case 'emoji':
            default:
                return encodeBase64ToEmoji(b64);
        }
    }

    function decodePayloadForCodec(payload, codecId) {
        switch (normalizeCodecId(codecId)) {
            case 'base64':
                return payload + '='.repeat((4 - (payload.length % 4)) % 4);
            case 'cyrillic':
                return decodeCyrillicToBase64(payload);
            case 'emoji':
            default:
                return decodeEmojiToBase64(payload);
        }
    }

    function isValidBase64Payload(payload) {
        return typeof payload === 'string'
            && payload.length >= 4
            && payload.length % 4 === 0
            && /^[A-Za-z0-9+/]+={0,2}$/.test(payload);
    }

    function isPlausibleEncodedPayload(payload, codecId) {
        if (typeof payload !== 'string' || !payload) return false;

        try {
            const b64 = decodePayloadForCodec(payload, codecId);
            return isValidBase64Payload(b64);
        } catch {
            return false;
        }
    }

    function toCompactKeyId(slotId) {
        const match = /^k([1-4])$/.exec(slotId);
        return match ? match[1] : slotId;
    }

    function fromCompactKeyId(compactId) {
        return /^[1-4]$/.test(compactId) ? `k${compactId}` : compactId;
    }

    function formatEncryptedMessage(slotId, payload, codecId) {
        const codec = getCipherCodecConfig(codecId);
        return `${FORMAT_START}${toCompactKeyId(slotId)}${FORMAT_MID}${codec.shortCode}${FORMAT_PAYLOAD}${payload}`;
    }

    function parseEncryptedMessage(text) {
        const trimmed = (text || '').trim();
        const compactMatch = new RegExp(`^${FORMAT_START}(.+?)${FORMAT_MID}([${CODEC_MARKERS.base64}${CODEC_MARKERS.emoji}${CODEC_MARKERS.cyrillic}])${FORMAT_PAYLOAD}(.+)$`, 'su').exec(trimmed);
        if (!compactMatch) return null;

        const parsed = {
            originalText: trimmed,
            keyId: fromCompactKeyId(compactMatch[1]),
            codecId: compactMatch[2] === CODEC_MARKERS.emoji
                ? 'emoji'
                : compactMatch[2] === CODEC_MARKERS.cyrillic
                    ? 'cyrillic'
                    : 'base64',
            encodedPayload: compactMatch[3]
        };

        return isPlausibleEncodedPayload(parsed.encodedPayload, parsed.codecId)
            ? parsed
            : null;
    }

    async function deriveKeyMaterialFromSeed(seedText) {
        const encoder = new TextEncoder();

        const baseKey = await crypto.subtle.importKey(
            'raw',
            encoder.encode(seedText),
            'PBKDF2',
            false,
            ['deriveBits']
        );

        const bits = await crypto.subtle.deriveBits(
            {
                name: 'PBKDF2',
                salt: encoder.encode(KDF_SALT),
                iterations: KDF_ITERATIONS,
                hash: 'SHA-256'
            },
            baseKey,
            1024
        );

        const bytes = new Uint8Array(bits);

        return {
            k1: bytesToHex(bytes.slice(0, 32)),
            k2: bytesToHex(bytes.slice(32, 64)),
            k3: bytesToHex(bytes.slice(64, 96)),
            k4: bytesToHex(bytes.slice(96, 128))
        };
    }

    async function deriveKeyFromName(name) {
        if (!name || !name.trim()) {
            throw new Error('Пустое слово');
        }
        const hash = await crypto.subtle.digest(
            'SHA-256',
            new TextEncoder().encode(name.trim())
        );
        return bytesToHex(new Uint8Array(hash));
    }

    async function encryptAESGCM(plainText, keyHex) {
        const key = await crypto.subtle.importKey(
            'raw',
            hexToBytes(keyHex),
            { name: 'AES-GCM' },
            false,
            ['encrypt']
        );

        const iv = crypto.getRandomValues(new Uint8Array(IV_LEN));
        const data = new TextEncoder().encode(plainText);

        const encrypted = await crypto.subtle.encrypt(
            { name: 'AES-GCM', iv, tagLength: 128 },
            key,
            data
        );

        const encryptedArr = new Uint8Array(encrypted);
        const payload = new Uint8Array(iv.length + encryptedArr.length);

        payload.set(iv);
        payload.set(encryptedArr, iv.length);

        return bytesToBase64(payload);
    }

    async function decryptAESGCM(b64Payload, keyHex) {
        const data = base64ToBytes(b64Payload);

        if (data.length < IV_LEN + TAG_LEN) {
            throw new Error('Data too short');
        }

        const iv = data.slice(0, IV_LEN);
        const ciphertextWithTag = data.slice(IV_LEN);

        const key = await crypto.subtle.importKey(
            'raw',
            hexToBytes(keyHex),
            { name: 'AES-GCM' },
            false,
            ['decrypt']
        );

        const decrypted = await crypto.subtle.decrypt(
            { name: 'AES-GCM', iv, tagLength: 128 },
            key,
            ciphertextWithTag
        );

        return new TextDecoder().decode(decrypted);
    }

    async function encryptBinaryAESGCM(dataBytes, keyHex) {
        const key = await crypto.subtle.importKey(
            'raw',
            hexToBytes(keyHex),
            { name: 'AES-GCM' },
            false,
            ['encrypt']
        );

        const iv = crypto.getRandomValues(new Uint8Array(IV_LEN));
        const encrypted = await crypto.subtle.encrypt(
            { name: 'AES-GCM', iv, tagLength: 128 },
            key,
            dataBytes
        );

        return concatBytes([iv, new Uint8Array(encrypted)]);
    }

    async function decryptBinaryAESGCM(payloadBytes, keyHex) {
        if (payloadBytes.length < IV_LEN + TAG_LEN) {
            throw new Error('Media payload too short');
        }

        const key = await crypto.subtle.importKey(
            'raw',
            hexToBytes(keyHex),
            { name: 'AES-GCM' },
            false,
            ['decrypt']
        );

        const iv = payloadBytes.slice(0, IV_LEN);
        const ciphertextWithTag = payloadBytes.slice(IV_LEN);
        const decrypted = await crypto.subtle.decrypt(
            { name: 'AES-GCM', iv, tagLength: 128 },
            key,
            ciphertextWithTag
        );

        return new Uint8Array(decrypted);
    }

    function buildEncryptedMediaName(originalName) {
        const clean = String(originalName || 'media.bin').trim() || 'media.bin';
        return clean.endsWith(MEDIA_CONTAINER_EXT) ? clean : `${clean}${MEDIA_CONTAINER_EXT}`;
    }

    function isEncryptedMediaName(name) {
        return new RegExp(`${MEDIA_CONTAINER_EXT}(?:$|[?#])`, 'i').test(String(name || ''));
    }

    function isEncryptableMediaFile(file) {
        return Boolean(
            file &&
            typeof file.type === 'string' &&
            /^(image|audio|video)\//i.test(file.type)
        );
    }

    async function buildEncryptedMediaFile(file, keyHex, slotId) {
        const sourceBytes = new Uint8Array(await file.arrayBuffer());
        const encryptedPayload = await encryptBinaryAESGCM(sourceBytes, keyHex);
        const metadata = {
            version: 1,
            keyId: slotId,
            mime: file.type || 'application/octet-stream',
            originalName: file.name || 'media.bin',
            originalSize: file.size || sourceBytes.length
        };
        const metaBytes = utf8ToBytes(JSON.stringify(metadata));
        const header = concatBytes([
            utf8ToBytes(MEDIA_CONTAINER_MAGIC),
            uint32ToBytes(metaBytes.length),
            metaBytes
        ]);
        const containerBytes = concatBytes([header, encryptedPayload]);

        return new File(
            [containerBytes],
            buildEncryptedMediaName(file.name),
            {
                type: MEDIA_ENCRYPTED_MIME,
                lastModified: Date.now()
            }
        );
    }

    function parseEncryptedMediaContainer(bytes) {
        const magicBytes = utf8ToBytes(MEDIA_CONTAINER_MAGIC);

        if (bytes.length < magicBytes.length + 4 + IV_LEN + TAG_LEN) {
            throw new Error('Encrypted media container too short');
        }

        const actualMagic = bytesToUtf8(bytes.slice(0, magicBytes.length));
        if (actualMagic !== MEDIA_CONTAINER_MAGIC) {
            throw new Error('Unknown encrypted media format');
        }

        const metaLength = bytesToUint32(bytes, magicBytes.length);
        const metaStart = magicBytes.length + 4;
        const metaEnd = metaStart + metaLength;

        if (metaEnd > bytes.length) {
            throw new Error('Broken encrypted media metadata');
        }

        const metadata = JSON.parse(bytesToUtf8(bytes.slice(metaStart, metaEnd)));
        return {
            metadata,
            encryptedPayload: bytes.slice(metaEnd)
        };
    }

    function getAllKeys() {
        const all = {};

        if (DERIVED_KEYS) Object.assign(all, DERIVED_KEYS);
        if (CUSTOM_KEYS) {
            for (const [slot, info] of Object.entries(CUSTOM_KEYS)) {
                if (info && typeof info === 'object' && info.key) {
                    all[slot] = info.key;
                } else if (typeof info === 'string') {
                    all[slot] = info;
                }
            }
        }
        if (TEMP_KEY) all['@temp'] = TEMP_KEY;

        return all;
    }

    function getCustomKeyLabel(slot) {
        const info = CUSTOM_KEYS[slot];
        if (!info || typeof info !== 'object') return '';
        return info.label || '';
    }

    function getCurrentKeyHex() {
        return getAllKeys()[currentKeySlot] || null;
    }

    function hasAnyKeys() {
        return Boolean(DERIVED_KEYS || Object.keys(CUSTOM_KEYS).length || TEMP_KEY);
    }

    // ============================================================
    // Markerless Russian-word transport
    // ============================================================

    const WORDS_CODEC_ID = 1;
    const WORDS_PACKET_MAGIC = new Uint8Array([0x56, 0x4b, 0x57, 0x31]); // VKW1
    const WORDS_PACKET_HEADER_LEN = 32;
    const WORDS_GROUP_ID_LEN = 12;
    const WORDS_MAX_RAW_CHUNK = 1200;
    const WORD_FRAGMENT_GROUPS = new Map();
    const RU_WORDS_DICTIONARY_SET = new Set(RU_WORDS_DICTIONARY);
    let wordGroupsLoaded = false;

    function persistWordFragmentGroups() {
        try {
            const saved = Array.from(WORD_FRAGMENT_GROUPS.values())
                .filter(group => group.expiresAt > Date.now())
                .map(group => ({
                    key: group.key,
                    groupId: group.groupId,
                    partCount: group.partCount,
                    totalPlaintextLength: group.totalPlaintextLength,
                    compressed: group.compressed,
                    expiresAt: group.expiresAt,
                    parts: Array.from(group.parts.entries()).map(([index, part]) => ({
                        index,
                        payload: bytesToBase64(part.payload)
                    }))
                }));
            localStorage.setItem(WORDS_GROUP_STORAGE_KEY, JSON.stringify(saved));
        } catch {
            // Private browsing or disabled storage must not disable decryption.
        }
    }

    function loadWordFragmentGroups() {
        if (wordGroupsLoaded) return;
        wordGroupsLoaded = true;

        try {
            const saved = JSON.parse(localStorage.getItem(WORDS_GROUP_STORAGE_KEY) || '[]');
            saved.forEach(raw => {
                if (!raw || raw.expiresAt <= Date.now() || !raw.key) return;
                const group = {
                    ...raw,
                    parts: new Map(),
                    elements: new Map()
                };
                (raw.parts || []).forEach(part => {
                    if (Number.isInteger(part.index) && typeof part.payload === 'string') {
                        group.parts.set(part.index, {
                            partIndex: part.index,
                            payload: base64ToBytes(part.payload)
                        });
                    }
                });
                WORD_FRAGMENT_GROUPS.set(group.key, group);
            });
            persistWordFragmentGroups();
        } catch {
            localStorage.removeItem(WORDS_GROUP_STORAGE_KEY);
        }
    }

    function writeUint16(bytes, offset, value) {
        new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).setUint16(offset, value, false);
    }

    function readUint16(bytes, offset) {
        return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).getUint16(offset, false);
    }

    function isWordsDictionaryText(text) {
        const words = String(text || '').trim().split(/\s+/u);
        return words.length > 0 && words.every(word => RU_WORDS_DICTIONARY_SET.has(word));
    }

    function encodeBytesToWords(bytes, dictionary = RU_WORDS_DICTIONARY) {
        if (!Number.isInteger(bytes.length) || bytes.length > 0xffffffff) {
            throw new Error('Слишком большой словарный payload');
        }

        const source = concatBytes([uint32ToBytes(bytes.length), bytes]);
        const words = [];
        let accumulator = 0;
        let bitCount = 0;

        source.forEach(byte => {
            accumulator = (accumulator << 8) | byte;
            bitCount += 8;

            while (bitCount >= WORDS_BITS) {
                bitCount -= WORDS_BITS;
                words.push(dictionary[(accumulator >>> bitCount) & 0x1fff]);
                accumulator = bitCount ? accumulator & ((1 << bitCount) - 1) : 0;
            }
        });

        if (bitCount) {
            words.push(dictionary[(accumulator << (WORDS_BITS - bitCount)) & 0x1fff]);
        }

        return words.join(' ');
    }

    function decodeWordsToBytes(text, dictionary = RU_WORDS_DICTIONARY) {
        const words = String(text || '').trim().split(/\s+/u);
        if (!words.length || words.length > 10000) throw new Error('Недопустимое число слов');

        const indexes = words.map(word => dictionary.indexOf(word));
        if (indexes.some(index => index < 0)) throw new Error('Слово отсутствует в словаре');

        const output = [];
        let accumulator = 0;
        let bitCount = 0;

        indexes.forEach(index => {
            accumulator = (accumulator << WORDS_BITS) | index;
            bitCount += WORDS_BITS;

            while (bitCount >= 8) {
                bitCount -= 8;
                output.push((accumulator >>> bitCount) & 0xff);
                accumulator = bitCount ? accumulator & ((1 << bitCount) - 1) : 0;
            }
        });

        if (bitCount && accumulator !== 0) throw new Error('Ненулевые биты заполнения');
        if (output.length < 4) throw new Error('Словарный payload слишком короткий');

        const outputBytes = Uint8Array.from(output);
        const length = bytesToUint32(outputBytes, 0);
        const expectedLength = length + 4;
        if (outputBytes.length < expectedLength) throw new Error('Неверная длина словарного payload');
        if (outputBytes.slice(expectedLength).some(byte => byte !== 0)) {
            throw new Error('Ненулевые биты после словарного payload');
        }
        return outputBytes.slice(4, expectedLength);
    }

    async function compressTransportBytes(bytes) {
        if (typeof CompressionStream !== 'function') {
            return { bytes, compressed: false };
        }

        const stream = new Blob([bytes]).stream().pipeThrough(new CompressionStream('gzip'));
        const compressed = new Uint8Array(await new Response(stream).arrayBuffer());
        return compressed.length < bytes.length
            ? { bytes: compressed, compressed: true }
            : { bytes, compressed: false };
    }

    async function decompressTransportBytes(bytes) {
        if (typeof DecompressionStream !== 'function') {
            throw new Error('Эта платформа не умеет распаковывать gzip');
        }

        const stream = new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));
        return new Uint8Array(await new Response(stream).arrayBuffer());
    }

    function buildWordsPacket(payload, metadata) {
        const packet = new Uint8Array(WORDS_PACKET_HEADER_LEN + payload.length);
        packet.set(WORDS_PACKET_MAGIC, 0);
        packet[4] = 1;
        packet[5] = metadata.compressed ? 1 : 0;
        packet[6] = WORDS_CODEC_ID;
        packet[7] = 1;
        packet.set(metadata.groupId, 8);
        writeUint16(packet, 20, metadata.partIndex);
        writeUint16(packet, 22, metadata.partCount);
        new DataView(packet.buffer).setUint32(24, metadata.totalPlaintextLength, false);
        new DataView(packet.buffer).setUint32(28, payload.length, false);
        packet.set(payload, WORDS_PACKET_HEADER_LEN);
        return packet;
    }

    function parseWordsPacket(packet) {
        if (packet.length < WORDS_PACKET_HEADER_LEN) throw new Error('Пакет слишком короткий');
        if (!WORDS_PACKET_MAGIC.every((value, index) => packet[index] === value)) {
            throw new Error('Неизвестная версия словарного пакета');
        }
        if (packet[4] !== 1 || packet[6] !== WORDS_CODEC_ID || packet[7] !== 1) {
            throw new Error('Несовместимый словарный пакет');
        }

        const payloadLength = bytesToUint32(packet, 28);
        if (payloadLength !== packet.length - WORDS_PACKET_HEADER_LEN) {
            throw new Error('Повреждённая длина словарного пакета');
        }

        return {
            compressed: Boolean(packet[5] & 1),
            groupId: bytesToHex(packet.slice(8, 20)),
            partIndex: readUint16(packet, 20),
            partCount: readUint16(packet, 22),
            totalPlaintextLength: bytesToUint32(packet, 24),
            payload: packet.slice(WORDS_PACKET_HEADER_LEN)
        };
    }

    function buildWordsPacketText(payload, metadata, keyHex) {
        return encryptBinaryAESGCM(buildWordsPacket(payload, metadata), keyHex)
            .then(encrypted => encodeBytesToWords(encrypted));
    }

    async function fitWordsChunk(payload, start, metadata, partCount, maxRawChunk) {
        const remaining = payload.length - start;
        const upper = Math.min(maxRawChunk, remaining);

        for (let size = upper; size > 0; size -= 1) {
            const candidate = payload.slice(start, start + size);
            const text = await buildWordsPacketText(candidate, {
                ...metadata,
                partIndex: 0,
                partCount
            }, metadata.keyHex);

            if (text.length <= MAX_VK_UTF16_UNITS) return size;
        }

        throw new Error('Даже минимальная часть не помещается в лимит VK');
    }

    async function encodePlaintextToWordMessages(plainText, keyHex) {
        const plainBytes = utf8ToBytes(plainText);
        const compressed = await compressTransportBytes(plainBytes);
        const groupId = crypto.getRandomValues(new Uint8Array(WORDS_GROUP_ID_LEN));
        const metadata = {
            compressed: compressed.compressed,
            groupId,
            totalPlaintextLength: plainBytes.length,
            keyHex
        };
        let maxRawChunk = WORDS_MAX_RAW_CHUNK;

        for (let attempt = 0; attempt < 32 && maxRawChunk > 0; attempt += 1) {
            let partCount = Math.max(1, Math.ceil(compressed.bytes.length / maxRawChunk));
            let chunks = [];

            for (let pass = 0; pass < 4; pass += 1) {
                chunks = [];
                let offset = 0;

                while (offset < compressed.bytes.length) {
                    const size = await fitWordsChunk(
                        compressed.bytes,
                        offset,
                        metadata,
                        partCount,
                        maxRawChunk
                    );
                    chunks.push(compressed.bytes.slice(offset, offset + size));
                    offset += size;
                }

                if (chunks.length === partCount) break;
                partCount = chunks.length;
            }

            const messages = await Promise.all(chunks.map((chunk, partIndex) => buildWordsPacketText(chunk, {
                ...metadata,
                partIndex,
                partCount: chunks.length
            }, keyHex)));

            if (messages.every(message => message.length <= MAX_VK_UTF16_UNITS)) {
                return messages;
            }

            // AES-GCM uses a fresh nonce for each attempt, so reserve margin
            // and validate the exact final strings before returning them.
            maxRawChunk -= 32;
        }

        throw new Error('Не удалось безопасно разбить словарное сообщение');
    }

    async function decryptWordMessage(text, keyHex) {
        if (!isWordsDictionaryText(text)) return null;

        const encrypted = decodeWordsToBytes(text);
        const packet = parseWordsPacket(await decryptBinaryAESGCM(encrypted, keyHex));
        if (!packet.partCount || packet.partIndex >= packet.partCount) {
            throw new Error('Неверная нумерация частей');
        }

        return packet;
    }

    async function completeWordGroup(group) {
        const ordered = [];
        for (let index = 0; index < group.partCount; index += 1) {
            const part = group.parts.get(index);
            if (!part) return null;
            ordered.push(part.payload);
        }

        const payload = concatBytes(ordered);
        const plainBytes = group.compressed
            ? await decompressTransportBytes(payload)
            : payload;

        if (plainBytes.length !== group.totalPlaintextLength) {
            throw new Error('Неверная длина исходного текста');
        }

        WORD_FRAGMENT_GROUPS.delete(group.key);
        persistWordFragmentGroups();
        return bytesToUtf8(plainBytes);
    }

    async function acceptWordFragment(packet, msgEl, originalText) {
        loadWordFragmentGroups();
        const key = `${currentChatContextId}:${packet.groupId}`;
        let group = WORD_FRAGMENT_GROUPS.get(key);

        if (!group || group.partCount !== packet.partCount || group.totalPlaintextLength !== packet.totalPlaintextLength) {
            group = {
                key,
                groupId: packet.groupId,
                partCount: packet.partCount,
                totalPlaintextLength: packet.totalPlaintextLength,
                compressed: packet.compressed,
                parts: new Map(),
                elements: new Map(),
                expiresAt: Date.now() + WORDS_GROUP_TTL_MS
            };
            WORD_FRAGMENT_GROUPS.set(key, group);
        }

        if (group.expiresAt < Date.now()) {
            WORD_FRAGMENT_GROUPS.delete(key);
            return null;
        }

        group.parts.set(packet.partIndex, packet);
        group.elements.set(packet.partIndex, msgEl);
        persistWordFragmentGroups();

        if (group.partCount > 1 && group.parts.size < group.partCount) {
            createFragmentStatusInterface(msgEl, group.parts.size, group.partCount, originalText);
            return null;
        }

        const decryptedText = await completeWordGroup(group);
        if (decryptedText === null) return null;

        const first = group.elements.get(0) || msgEl;
        createToggleInterface(originalText, decryptedText, first);
        group.elements.forEach((element, index) => {
            if (index !== 0) {
                element.textContent = '';
                element.dataset.vkdecDone = 'true';
                element.hidden = true;
            }
        });
        return decryptedText;
    }
    // ============================================================
    // Styles
    // ============================================================

    function injectStyles() {
        if (document.getElementById('vk-p2p-styles')) return;

        const style = document.createElement('style');
        style.id = 'vk-p2p-styles';
        style.textContent = `
            @keyframes vkP2PFadeIn {
                from { opacity: 0; transform: translateY(8px) scale(0.98); }
                to { opacity: 1; transform: translateY(0) scale(1); }
            }

            @keyframes vkP2PToastOut {
                0% { opacity: 1; transform: translate(-50%, 0); }
                75% { opacity: 1; transform: translate(-50%, 0); }
                100% { opacity: 0; transform: translate(-50%, 12px); }
            }

            .vk-p2p-overlay {
                position: fixed;
                inset: 0;
                background: rgba(0, 0, 0, 0.62);
                z-index: 999999;
                display: flex;
                align-items: center;
                justify-content: center;
                padding: 16px;
                box-sizing: border-box;
                backdrop-filter: blur(4px);
            }

            .vk-p2p-modal {
                width: min(480px, 100%);
                max-height: calc(100vh - 32px);
                overflow-y: auto;
                background: #ffffff;
                color: #111827;
                border-radius: 18px;
                box-shadow: 0 24px 80px rgba(0, 0, 0, 0.35);
                padding: 20px;
                box-sizing: border-box;
                animation: vkP2PFadeIn 0.18s ease-out;
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
            }

            .vk-p2p-modal h3 {
                margin: 0 0 8px;
                font-size: 18px;
                line-height: 1.25;
                font-weight: 700;
            }

            .vk-p2p-modal p {
                margin: 0 0 12px;
                font-size: 13px;
                line-height: 1.45;
                color: #4b5563;
            }

            .vk-p2p-row {
                display: flex;
                gap: 8px;
                align-items: center;
            }

            .vk-p2p-input,
            .vk-p2p-select,
            .vk-p2p-textarea {
                width: 100%;
                box-sizing: border-box;
                border: 1px solid #d1d5db;
                background: #fff;
                color: #111827;
                border-radius: 10px;
                padding: 11px 12px;
                font-size: 14px;
                outline: none;
                transition: border-color 0.15s, box-shadow 0.15s;
            }

            .vk-p2p-input:focus,
            .vk-p2p-select:focus,
            .vk-p2p-textarea:focus {
                border-color: #2688eb;
                box-shadow: 0 0 0 3px rgba(38, 136, 235, 0.15);
            }

            .vk-p2p-textarea {
                min-height: 84px;
                resize: vertical;
                font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
            }

            .vk-p2p-check {
                display: flex;
                align-items: flex-start;
                gap: 8px;
                font-size: 13px;
                line-height: 1.35;
                color: #374151;
                margin: 8px 0 12px;
                user-select: none;
            }

            .vk-p2p-check input {
                margin-top: 2px;
            }

            .vk-p2p-actions {
                display: flex;
                justify-content: flex-end;
                gap: 8px;
                margin-top: 14px;
                flex-wrap: wrap;
            }

            .vk-p2p-btn {
                border: none;
                border-radius: 10px;
                padding: 9px 13px;
                font-size: 13px;
                cursor: pointer;
                transition: transform 0.08s, opacity 0.15s, background 0.15s;
                white-space: nowrap;
            }

            .vk-p2p-btn:active {
                transform: translateY(1px);
            }

            .vk-p2p-btn:disabled {
                opacity: 0.55;
                cursor: default;
            }

            .vk-p2p-btn-primary {
                background: #2688eb;
                color: #fff;
            }

            .vk-p2p-btn-secondary {
                background: #f3f4f6;
                color: #111827;
            }

            .vk-p2p-btn-danger {
                background: #fee2e2;
                color: #991b1b;
            }

            .vk-p2p-eye-btn {
                min-width: 44px;
                padding-left: 10px;
                padding-right: 10px;
            }

            .vk-p2p-error {
                display: none;
                color: #b91c1c !important;
                font-size: 12px !important;
                margin-top: 8px !important;
            }

            .vk-p2p-note {
                border-radius: 12px;
                padding: 10px 12px;
                background: #f3f7ff;
                color: #31527a !important;
                font-size: 12px !important;
            }

            .vk-p2p-controls {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                gap: 2px;
                margin-right: 2px;
                align-self: flex-end;
                min-width: 36px;
                min-height: 36px;
                vertical-align: middle;
            }

            .vk-p2p-icon-btn {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 36px;
                height: 36px;
                background: transparent;
                border: none;
                cursor: pointer;
                color: inherit;
                opacity: 0.58;
                padding: 0;
                border-radius: 8px;
                line-height: 1;
                transition: opacity 0.15s, background 0.15s;
                vertical-align: middle;
            }

            .vk-p2p-icon-glyph {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 100%;
                height: 100%;
                line-height: 1;
                transform: translateY(-1px);
                pointer-events: none;
            }

            .vk-p2p-icon-btn:hover {
                opacity: 1;
                background: rgba(127, 127, 127, 0.10);
            }

            .vk-p2p-icon-btn-main {
                font-size: 18px;
            }

            .vk-p2p-icon-btn-small {
                font-size: 18px;
            }

            .vk-p2p-menu {
                position: fixed;
                z-index: 999999;
                box-sizing: border-box;
                width: min(340px, calc(100vw - 16px));
                max-width: calc(100vw - 16px);
                max-height: calc(100vh - 16px);
                overflow-y: auto;
                padding: 8px;
                border-radius: 14px;
                background: #ffffff;
                color: #111827;
                box-shadow: 0 18px 48px rgba(0,0,0,0.24);
                border: 1px solid rgba(0,0,0,0.10);
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
                font-size: 13px;
                animation: vkP2PFadeIn 0.12s ease-out;
            }

            .vk-p2p-menu-title {
                padding: 7px 9px 6px;
                color: #6b7280;
                font-size: 12px;
            }

            .vk-p2p-menu-item {
                display: block;
                width: 100%;
                border: none;
                background: transparent;
                color: inherit;
                text-align: left;
                padding: 9px 10px;
                border-radius: 9px;
                cursor: pointer;
                font: inherit;
                overflow: hidden;
                text-overflow: ellipsis;
                white-space: nowrap;
                max-width: 100%;
            }

            .vk-p2p-menu-field {
                display: grid;
                gap: 6px;
                padding: 8px 10px;
            }

            .vk-p2p-menu-label {
                color: #4b5563;
                font-size: 12px;
            }

            .vk-p2p-menu-select {
                width: 100%;
                box-sizing: border-box;
                border: 1px solid #d1d5db;
                border-radius: 9px;
                padding: 8px 10px;
                background: #fff;
                color: #111827;
                font: inherit;
            }

            .vk-p2p-menu-item:hover {
                background: #f3f4f6;
            }

            .vk-p2p-menu-item-active {
                background: #e8f1ff;
                color: #155aa3;
            }

            .vk-p2p-menu-sep {
                border-top: 1px solid #eef0f3;
                margin: 6px 0;
            }

            .vk-p2p-menu-danger {
                color: #b91c1c;
            }

            .vk-p2p-toast {
                position: fixed;
                left: 50%;
                bottom: 22px;
                transform: translateX(-50%);
                background: #1f2937;
                color: #fff;
                padding: 10px 14px;
                border-radius: 12px;
                font-size: 13px;
                z-index: 1000000;
                box-shadow: 0 8px 28px rgba(0,0,0,0.25);
                animation: vkP2PToastOut 2.4s forwards;
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
            }

            .vk-p2p-voice-status {
                display: inline-flex;
                align-items: center;
                min-width: 62px;
                height: 28px;
                padding: 0 9px;
                border-radius: 14px;
                background: rgba(213, 53, 67, 0.14);
                color: #d53543;
                font: 600 12px/1 ui-monospace, SFMono-Regular, Menlo, monospace;
                white-space: nowrap;
            }

            .vk-p2p-voice-recording {
                color: #d53543 !important;
                animation: vk-p2p-voice-pulse 1.2s ease-in-out infinite;
            }

            @keyframes vk-p2p-voice-pulse {
                50% { opacity: 0.45; }
            }

            .vk-dec-content {
                white-space: pre-wrap;
            }

            .vk-dec-toggle {
                display: inline-block;
                margin-left: 8px;
                font-size: 11px;
                text-decoration: underline;
                cursor: pointer;
                opacity: 0.65;
                user-select: none;
                color: inherit;
            }

            .vk-dec-toggle:hover {
                opacity: 1;
            }

            .vk-dec-error {
                display: block;
                margin-top: 6px;
                font-size: 12px;
                line-height: 1.35;
                color: rgba(255, 255, 255, 0.72);
            }

            .vk-p2p-media-box {
                display: flex;
                flex-direction: column;
                gap: 8px;
                margin-top: 8px;
                padding: 10px 12px;
                border-radius: 12px;
                background: rgba(38, 136, 235, 0.10);
                max-width: min(520px, 100%);
            }

            .vk-p2p-media-actions {
                display: flex;
                align-items: center;
                gap: 8px;
                flex-wrap: wrap;
            }

            .vk-p2p-media-btn,
            .vk-p2p-media-download {
                border: none;
                border-radius: 999px;
                padding: 6px 10px;
                font-size: 12px;
                line-height: 1.2;
                cursor: pointer;
                background: rgba(38, 136, 235, 0.18);
                color: inherit;
                text-decoration: none;
            }

            .vk-p2p-media-download[hidden] {
                display: none !important;
            }

            .vk-p2p-media-meta {
                font-size: 12px;
                opacity: 0.72;
                word-break: break-word;
            }

            .vk-p2p-media-preview img,
            .vk-p2p-media-preview video {
                display: block;
                max-width: min(420px, 100%);
                border-radius: 10px;
            }

            .vk-p2p-media-preview audio {
                width: min(420px, 100%);
            }

            .vk-p2p-media-error {
                font-size: 12px;
                line-height: 1.35;
                color: #b91c1c;
            }
        `;

        document.head.appendChild(style);
    }
    // ============================================================
    // UI helpers
    // ============================================================

    function showToast(text) {
        injectStyles();

        const old = document.querySelector('.vk-p2p-toast');
        if (old) old.remove();

        const toast = document.createElement('div');
        toast.className = 'vk-p2p-toast';
        toast.textContent = text;

        document.body.appendChild(toast);
        setTimeout(() => toast.remove(), 2500);
    }

    function createModal({ title, bodyHtml, actionsHtml = '', closeOnOverlay = true }) {
        injectStyles();

        const overlay = document.createElement('div');
        overlay.className = 'vk-p2p-overlay';

        const modal = document.createElement('div');
        modal.className = 'vk-p2p-modal';

        modal.innerHTML = `
            <h3>${title}</h3>
            ${bodyHtml}
            ${actionsHtml ? `<div class="vk-p2p-actions">${actionsHtml}</div>` : ''}
        `;

        overlay.appendChild(modal);
        document.body.appendChild(overlay);

        if (closeOnOverlay) {
            overlay.addEventListener('click', (e) => {
                if (e.target === overlay) overlay.remove();
            });
        }

        return { overlay, modal };
    }

    function attachPasswordEye(input, eyeBtn) {
        eyeBtn.addEventListener('click', () => {
            input.type = input.type === 'password' ? 'text' : 'password';
            eyeBtn.textContent = input.type === 'password' ? '👁️' : '🙈';
            input.focus();
        });
    }

    function closeMenus() {
        document.querySelectorAll('.vk-p2p-menu').forEach(el => el.remove());
    }

    function truncateForDisplay(s, max = 16) {
        if (!s) return '';
        if (s.length <= max) return s;
        return s.slice(0, max - 2) + '..';
    }

    function formatKeyDisplay(slotId) {
        if (slotId === '@temp') return '⚡ @temp — временный';
        if (['k1', 'k2', 'k3', 'k4'].includes(slotId)) return `🔑 ${slotId}`;

        const label = getCustomKeyLabel(slotId);
        if (!label) return `🔑 ${slotId}`;
        return `🔑 ${slotId} (${truncateForDisplay(label)})`;
    }

    // ============================================================
    // Setup modal
    // ============================================================

    function showSeedSetupModal() {
        if (document.querySelector('.vk-p2p-overlay')) return;

        const { overlay, modal } = createModal({
            title: '🔐 Настройка VKEncrypt',
            closeOnOverlay: true,
            bodyHtml: `
                <p>
                    Введите секретное слово, число или фразу. Из неё будут созданы одинаковые ключи
                    <b>k1–k4</b> на всех устройствах, где введена та же фраза.
                </p>

                <p class="vk-p2p-note">
                    Лучше использовать длинную фразу из нескольких слов. Простые числа вроде <b>1234</b>
                    легко перебираются. Фраза не сохраняется — сохраняются только производные ключи.
                </p>

                <div class="vk-p2p-row">
                    <input class="vk-p2p-input" id="vk-p2p-seed-input" type="password"
                        placeholder="Например: длинная секретная фраза">
                    <button class="vk-p2p-btn vk-p2p-btn-secondary vk-p2p-eye-btn" id="vk-p2p-seed-eye" type="button">👁️</button>
                </div>

                <label class="vk-p2p-check">
                    <input id="vk-p2p-save-derived" type="checkbox" checked>
                    <span>Сохранить производные ключи на этом устройстве</span>
                </label>

                <label class="vk-p2p-check">
                    <input id="vk-p2p-auto-encrypt-first" type="checkbox">
                    <span>Включить автошифрование при отправке</span>
                </label>

                <label class="vk-p2p-check" for="vk-p2p-codec-first">
                    <span>Кодирование шифротекста</span>
                </label>
                <select class="vk-p2p-select" id="vk-p2p-codec-first">
                    <option value="emoji">Emoji</option>
                    <option value="cyrillic">Русский алфавит</option>
                    <option value="words">Русские слова (экспериментально)</option>
                    <option value="base64">Base64</option>
                </select>

                <p class="vk-p2p-error" id="vk-p2p-seed-error"></p>
            `,
            actionsHtml: `
                <button class="vk-p2p-btn vk-p2p-btn-secondary" id="vk-p2p-seed-temp">
                    Только на эту сессию
                </button>
                <button class="vk-p2p-btn vk-p2p-btn-primary" id="vk-p2p-seed-apply">
                    Создать ключи
                </button>
            `
        });

        const input = modal.querySelector('#vk-p2p-seed-input');
        const eyeBtn = modal.querySelector('#vk-p2p-seed-eye');
        const error = modal.querySelector('#vk-p2p-seed-error');
        const saveCheckbox = modal.querySelector('#vk-p2p-save-derived');
        const autoCheckbox = modal.querySelector('#vk-p2p-auto-encrypt-first');
        const codecSelect = modal.querySelector('#vk-p2p-codec-first');
        const applyBtn = modal.querySelector('#vk-p2p-seed-apply');
        const tempBtn = modal.querySelector('#vk-p2p-seed-temp');

        autoCheckbox.checked = settings.autoEncrypt;
        codecSelect.value = normalizeCodecId(settings.cipherCodec);

        attachPasswordEye(input, eyeBtn);

        setTimeout(() => input.focus(), 80);

        async function applySeed(saveMode) {
            const seed = input.value.trim();

            if (seed.length < 6) {
                error.textContent = 'Слишком коротко. Лучше минимум 12 символов или несколько слов.';
                error.style.display = 'block';
                return;
            }

            error.style.display = 'none';
            applyBtn.disabled = true;
            tempBtn.disabled = true;
            applyBtn.textContent = 'Создаю...';

            try {
                const keys = await deriveKeyMaterialFromSeed(seed);

                DERIVED_KEYS = keys;
                selectKeySlot(DEFAULT_KEY_SLOT, { rememberForChat: true });

                settings.autoEncrypt = Boolean(autoCheckbox.checked);
                settings.cipherCodec = normalizeCodecId(codecSelect.value);
                settings.saveDerivedKeys = Boolean(saveMode);
                saveSettings();

                if (saveMode) saveDerivedKeys(keys);

                overlay.remove();
                updateEncryptButtonsTitle();
                scan();

                showToast(saveMode ? '✅ Ключи созданы и сохранены' : '✅ Ключи созданы до перезагрузки страницы');
            } catch (err) {
                error.textContent = 'Ошибка генерации ключей: ' + err.message;
                error.style.display = 'block';
            } finally {
                applyBtn.disabled = false;
                tempBtn.disabled = false;
                applyBtn.textContent = 'Создать ключи';
            }
        }

        applyBtn.addEventListener('click', () => applySeed(saveCheckbox.checked));
        tempBtn.addEventListener('click', () => applySeed(false));

        input.addEventListener('keydown', (e) => {
            if (e.key === 'Enter') {
                e.preventDefault();
                applySeed(saveCheckbox.checked);
            }
        });
    }
    // ============================================================
    // Custom key modals
    // ============================================================

    function showAddCustomKeyModal() {
        const { overlay, modal } = createModal({
            title: '➕ Пользовательский ключ',
            bodyHtml: `
                <p>
                    Введи имя для слота и <b>64 hex-символа</b> — или просто любое слово
                    (например, «собака»). Из слова скрипт детерминированно выведет
                    256-битный ключ. Собеседнику нужно ввести то же слово.
                </p>

                <input class="vk-p2p-input" id="vk-p2p-custom-name"
                    placeholder="Имя слота, например k5, друг или friend1">

                <div style="height:8px"></div>

                <textarea class="vk-p2p-textarea" id="vk-p2p-custom-key"
                    placeholder="64 hex-символа ИЛИ любое слово: собака, мой-друг, ..."></textarea>

                <p class="vk-p2p-note">
                    Имя слота может быть и на кириллице. Подходят буквы любого алфавита, цифры, _, -, . и @.
                </p>

                <p class="vk-p2p-error" id="vk-p2p-custom-error"></p>
            `,
            actionsHtml: `
                <button class="vk-p2p-btn vk-p2p-btn-secondary" id="vk-p2p-custom-cancel">Отмена</button>
                <button class="vk-p2p-btn vk-p2p-btn-primary" id="vk-p2p-custom-save">Сохранить</button>
            `
        });

        const nameInput = modal.querySelector('#vk-p2p-custom-name');
        const keyInput = modal.querySelector('#vk-p2p-custom-key');
        const error = modal.querySelector('#vk-p2p-custom-error');
        const saveBtn = modal.querySelector('#vk-p2p-custom-save');
        const cancelBtn = modal.querySelector('#vk-p2p-custom-cancel');

        setTimeout(() => nameInput.focus(), 80);

        cancelBtn.addEventListener('click', () => overlay.remove());

        async function handleSave() {
            let name = nameInput.value.trim();
            const keyOrWord = keyInput.value.trim();

            if (!name) {
                error.textContent = 'Введите имя ключа.';
                error.style.display = 'block';
                return;
            }

            name = name.replace(/\s+/g, '_');

            if (['k1', 'k2', 'k3', 'k4', '@temp'].includes(name)) {
                error.textContent = 'Это имя зарезервировано. Используй другое.';
                error.style.display = 'block';
                return;
            }

            if (!/^[\p{L}\p{N}_.@-]{1,32}$/u.test(name)) {
                error.textContent = 'Имя может содержать буквы любого алфавита, цифры, _, -, . и @. До 32 символов.';
                error.style.display = 'block';
                return;
            }

            if (!keyOrWord) {
                error.textContent = 'Введите 64 hex-символа или любое слово.';
                error.style.display = 'block';
                return;
            }

            saveBtn.disabled = true;
            saveBtn.textContent = 'Создаю...';

            try {
                let keyHex;
                let label = '';

                if (isValidKeyHex(keyOrWord)) {
                    keyHex = keyOrWord.toLowerCase();
                } else {
                    keyHex = await deriveKeyFromName(keyOrWord);
                    label = keyOrWord;
                }

                CUSTOM_KEYS[name] = { key: keyHex, label };
                saveCustomKeys();
                selectKeySlot(name, { rememberForChat: true });

                overlay.remove();
                updateEncryptButtonsTitle();
                scan();

                const tag = label ? ` «${truncateForDisplay(label, 24)}»` : '';
                showToast(`✅ ${name}${tag} сохранён`);
            } catch (err) {
                error.textContent = 'Ошибка: ' + err.message;
                error.style.display = 'block';
            } finally {
                saveBtn.disabled = false;
                saveBtn.textContent = 'Сохранить';
            }
        }

        saveBtn.addEventListener('click', handleSave);

        keyInput.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && (e.ctrlKey || e.metaKey)) {
                e.preventDefault();
                handleSave();
            }
        });
    }

    async function generateTempKey() {
        const bytes = crypto.getRandomValues(new Uint8Array(32));
        const keyHex = bytesToHex(bytes);

        TEMP_KEY = keyHex;
        selectKeySlot('@temp', { rememberForChat: true });

        updateEncryptButtonsTitle();
        scan();

        try {
            await navigator.clipboard.writeText(keyHex);
            showToast('✅ Временный ключ создан и скопирован');
        } catch {
            showGeneratedKeyModal(keyHex);
        }
    }

    function showGeneratedKeyModal(keyHex) {
        const { overlay, modal } = createModal({
            title: '⚡ Новый временный ключ',
            bodyHtml: `
                <p>
                    Ключ создан и применён. Скопируй его и передай собеседнику.
                    Он исчезнет при перезагрузке страницы.
                </p>

                <textarea class="vk-p2p-textarea" id="vk-p2p-generated-key" readonly>${keyHex}</textarea>
            `,
            actionsHtml: `
                <button class="vk-p2p-btn vk-p2p-btn-secondary" id="vk-p2p-generated-close">Закрыть</button>
                <button class="vk-p2p-btn vk-p2p-btn-primary" id="vk-p2p-generated-copy">Скопировать</button>
            `
        });

        const output = modal.querySelector('#vk-p2p-generated-key');

        setTimeout(() => {
            output.focus();
            output.select();
        }, 80);

        modal.querySelector('#vk-p2p-generated-close').addEventListener('click', () => overlay.remove());

        modal.querySelector('#vk-p2p-generated-copy').addEventListener('click', async () => {
            try {
                await navigator.clipboard.writeText(keyHex);
                overlay.remove();
                showToast('✅ Ключ скопирован');
            } catch {
                output.focus();
                output.select();
            }
        });
    }

    function showSeedChangeModal() {
        const { overlay, modal } = createModal({
            title: '🔄 Сменить seed-фразу',
            bodyHtml: `
                <p>
                    Будут заново созданы ключи <b>k1–k4</b>. Старые сохранённые k1–k4 будут заменены.
                    Пользовательские ключи не удаляются.
                </p>

                <div class="vk-p2p-row">
                    <input class="vk-p2p-input" id="vk-p2p-change-seed-input" type="password"
                        placeholder="Новая секретная фраза">
                    <button class="vk-p2p-btn vk-p2p-btn-secondary vk-p2p-eye-btn" id="vk-p2p-change-seed-eye" type="button">👁️</button>
                </div>

                <label class="vk-p2p-check">
                    <input id="vk-p2p-change-save" type="checkbox" checked>
                    <span>Сохранить производные ключи на этом устройстве</span>
                </label>

                <p class="vk-p2p-error" id="vk-p2p-change-seed-error"></p>
            `,
            actionsHtml: `
                <button class="vk-p2p-btn vk-p2p-btn-secondary" id="vk-p2p-change-cancel">Отмена</button>
                <button class="vk-p2p-btn vk-p2p-btn-primary" id="vk-p2p-change-apply">Сменить</button>
            `
        });

        const input = modal.querySelector('#vk-p2p-change-seed-input');
        const eyeBtn = modal.querySelector('#vk-p2p-change-seed-eye');
        const error = modal.querySelector('#vk-p2p-change-seed-error');
        const saveCheckbox = modal.querySelector('#vk-p2p-change-save');
        const applyBtn = modal.querySelector('#vk-p2p-change-apply');

        attachPasswordEye(input, eyeBtn);

        setTimeout(() => input.focus(), 80);

        modal.querySelector('#vk-p2p-change-cancel').addEventListener('click', () => overlay.remove());

        async function apply() {
            const seed = input.value.trim();

            if (seed.length < 6) {
                error.textContent = 'Слишком коротко. Лучше минимум 12 символов или несколько слов.';
                error.style.display = 'block';
                return;
            }

            applyBtn.disabled = true;
            applyBtn.textContent = 'Создаю...';

            try {
                const keys = await deriveKeyMaterialFromSeed(seed);
                DERIVED_KEYS = keys;
                selectKeySlot(DEFAULT_KEY_SLOT, { rememberForChat: true });

                if (saveCheckbox.checked) {
                    saveDerivedKeys(keys);
                } else {
                    clearDerivedKeys();
                    DERIVED_KEYS = keys;
                }

                settings.saveDerivedKeys = Boolean(saveCheckbox.checked);
                saveSettings();

                overlay.remove();
                updateEncryptButtonsTitle();
                scan();

                showToast('✅ Seed-фраза сменена');
            } catch (err) {
                error.textContent = 'Ошибка: ' + err.message;
                error.style.display = 'block';
            } finally {
                applyBtn.disabled = false;
                applyBtn.textContent = 'Сменить';
            }
        }

        applyBtn.addEventListener('click', apply);

        input.addEventListener('keydown', (e) => {
            if (e.key === 'Enter') {
                e.preventDefault();
                apply();
            }
        });
    }
    // ============================================================
    // Incoming decrypt
    // ============================================================

    function createToggleInterface(originalEnc, decryptedText, parentEl) {
        parentEl.innerHTML = '';
        parentEl.dataset.vkdecOriginal = originalEnc;

        const textSpan = document.createElement('span');
        textSpan.className = 'vk-dec-content';
        textSpan.dataset.vkdecSkip = 'true';
        textSpan.textContent = decryptedText;
        textSpan.style.fontWeight = 'normal';

        const toggleLink = document.createElement('a');
        toggleLink.href = '#';
        toggleLink.className = 'vk-dec-toggle';
        toggleLink.dataset.vkdecSkip = 'true';
        toggleLink.textContent = '[шифр]';
        toggleLink.title = 'Показать зашифрованный оригинал';

        toggleLink.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();

            if (toggleLink.textContent === '[шифр]') {
                textSpan.textContent = originalEnc;
                textSpan.style.fontFamily = 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace';
                toggleLink.textContent = '[текст]';
            } else {
                textSpan.textContent = decryptedText;
                textSpan.style.fontFamily = '';
                toggleLink.textContent = '[шифр]';
            }
        });

        parentEl.appendChild(textSpan);
        parentEl.appendChild(document.createTextNode(' '));
        parentEl.appendChild(toggleLink);

        parentEl.dataset.vkdecDone = 'true';
    }

    function createErrorInterface(originalEnc, errorText, parentEl) {
        parentEl.innerHTML = '';
        parentEl.dataset.vkdecOriginal = originalEnc;

        const rawSpan = document.createElement('span');
        rawSpan.className = 'vk-dec-content';
        rawSpan.dataset.vkdecSkip = 'true';
        rawSpan.textContent = originalEnc;

        const errorLine = document.createElement('span');
        errorLine.className = 'vk-dec-error';
        errorLine.dataset.vkdecSkip = 'true';
        errorLine.textContent = `ошибка: ${errorText}`;

        parentEl.appendChild(rawSpan);
        parentEl.appendChild(errorLine);
        parentEl.dataset.vkdecDone = 'true';
    }

    function createFragmentStatusInterface(parentEl, received, total, originalText) {
        parentEl.innerHTML = '';
        parentEl.dataset.vkdecOriginal = originalText;

        const status = document.createElement('span');
        status.className = 'vk-dec-fragment-status';
        status.dataset.vkdecSkip = 'true';
        status.textContent = `Получена часть зашифрованного сообщения: ${received} из ${total}`;
        parentEl.appendChild(status);
        parentEl.dataset.vkdecDone = 'true';
    }

    function formatByteSize(size) {
        const value = Number(size) || 0;
        if (value < 1024) return `${value} B`;
        if (value < 1024 * 1024) return `${(value / 1024).toFixed(1)} KB`;
        return `${(value / (1024 * 1024)).toFixed(1)} MB`;
    }

    function getMediaLinkName(link) {
        if (!link) return '';

        const headline = link.querySelector('.AttachmentCell__headline, h4');
        if (headline?.textContent) {
            return headline.textContent.trim();
        }

        return (link.textContent || '').trim();
    }

    function setMediaLinkName(link, name) {
        if (!link) return;

        const headline = link.querySelector('.AttachmentCell__headline, h4');
        if (headline) {
            headline.textContent = name;
            return;
        }

        link.textContent = name;
    }

    function setMediaLinkFootnote(link, metadata, sizeBytes) {
        if (!link) return;

        const footnote = link.querySelector('.AttachmentCell__footnote');
        if (!footnote) return;

        const ext = (String(metadata?.originalName || '').split('.').pop() || 'BIN').toUpperCase();
        footnote.textContent = `${ext} ᐧ ${formatByteSize(sizeBytes)}`;
    }

    function isEncryptedMediaLink(link) {
        const name = getMediaLinkName(link);
        const href = link.getAttribute('href') || '';
        const footnote = (link.querySelector('.AttachmentCell__footnote')?.textContent || '').trim();

        return (
            /\.vke$/i.test(name) ||
            /\.vke($|[?#])/i.test(href) ||
            (/\.vke/i.test(name) && /\bVKE\b/i.test(footnote))
        );
    }

    function getEncryptedMediaLinks() {
        const links = new Set();

        document.querySelectorAll('a[href]').forEach(link => {
            if (link.closest('.vk-p2p-media-box')) return;
            if (link.dataset.vkP2PMediaLink === 'true') {
                links.add(link);
                return;
            }

            if (!isEncryptedMediaLink(link)) return;

            const container = link.closest(
                'article, .ConvoMessage, .ConvoMessage__text, .MessageText, .im_msg_text, .im-message--text, [role="listitem"], .Attachments'
            );
            if (!container) return;

            links.add(link);
        });

        return links;
    }

    function ensureMediaInterface(link) {
        if (link.dataset.vkP2PMediaLink === 'true') {
            const existing = link.parentElement?.querySelector('.vk-p2p-media-box');
            if (existing) return existing;
        }

        if (!link.dataset.vkP2POriginalHref) {
            link.dataset.vkP2POriginalHref = link.getAttribute('href') || '';
            link.dataset.vkP2POriginalText = getMediaLinkName(link);
            link.dataset.vkP2POriginalFootnote = (link.querySelector('.AttachmentCell__footnote')?.textContent || '').trim();
        }

        const box = document.createElement('div');
        box.className = 'vk-p2p-media-box';
        box.dataset.vkdecSkip = 'true';

        const actions = document.createElement('div');
        actions.className = 'vk-p2p-media-actions';

        const decryptBtn = document.createElement('button');
        decryptBtn.type = 'button';
        decryptBtn.className = 'vk-p2p-media-btn';
        decryptBtn.textContent = '🔓 Расшифровать вложение';

        const downloadLink = document.createElement('a');
        downloadLink.className = 'vk-p2p-media-download';
        downloadLink.textContent = 'Скачать';
        downloadLink.hidden = true;
        downloadLink.target = '_blank';
        downloadLink.rel = 'noopener noreferrer';

        const meta = document.createElement('div');
        meta.className = 'vk-p2p-media-meta';
        meta.textContent = 'Зашифрованное вложение VKEncrypt';

        const preview = document.createElement('div');
        preview.className = 'vk-p2p-media-preview';

        const error = document.createElement('div');
        error.className = 'vk-p2p-media-error';

        actions.appendChild(decryptBtn);
        actions.appendChild(downloadLink);
        box.appendChild(actions);
        box.appendChild(meta);
        box.appendChild(preview);
        box.appendChild(error);

        decryptBtn.addEventListener('click', () => {
            decryptIncomingMediaLink(link, { manual: true });
        });

        link.insertAdjacentElement('afterend', box);
        link.dataset.vkP2PMediaLink = 'true';
        return box;
    }

    function restoreMediaLink(link) {
        if (!link) return;

        const originalHref = link.dataset.vkP2POriginalHref;
        const originalText = link.dataset.vkP2POriginalText;
        const originalFootnote = link.dataset.vkP2POriginalFootnote;

        if (typeof originalHref === 'string') {
            link.setAttribute('href', originalHref);
        }

        if (typeof originalText === 'string') {
            setMediaLinkName(link, originalText);
        }

        const footnote = link.querySelector('.AttachmentCell__footnote');
        if (footnote && typeof originalFootnote === 'string') {
            footnote.textContent = originalFootnote;
        }

        link.removeAttribute('download');
    }

    function getMediaCacheKey(link) {
        return link?.dataset?.vkP2POriginalHref || link?.getAttribute('href') || '';
    }

    function getCachedDecryptedMedia(link) {
        const key = getMediaCacheKey(link);
        return key ? MEDIA_DECRYPT_CACHE.get(key) || null : null;
    }

    function setCachedDecryptedMedia(link, value) {
        const key = getMediaCacheKey(link);
        if (!key) return;
        MEDIA_DECRYPT_CACHE.set(key, value);
    }

    function resetMediaInterface(box, options = {}) {
        if (!box) return;
        const { preserveAutoTried = false } = options;

        const link = box.previousElementSibling?.matches?.('a[href]')
            ? box.previousElementSibling
            : null;

        const preview = box.querySelector('.vk-p2p-media-preview');
        const error = box.querySelector('.vk-p2p-media-error');
        const downloadLink = box.querySelector('.vk-p2p-media-download');
        const decryptBtn = box.querySelector('.vk-p2p-media-btn');
        const meta = box.querySelector('.vk-p2p-media-meta');

        if (preview?.dataset.vkP2PObjectUrl) {
            delete preview.dataset.vkP2PObjectUrl;
        }

        if (preview) preview.innerHTML = '';
        if (error) error.textContent = '';
        if (downloadLink) {
            downloadLink.hidden = true;
            downloadLink.removeAttribute('href');
            downloadLink.removeAttribute('download');
        }
        if (decryptBtn) {
            decryptBtn.disabled = false;
            decryptBtn.hidden = false;
            decryptBtn.textContent = '🔓 Расшифровать вложение';
            decryptBtn.removeAttribute('title');
        }
        if (meta) {
            meta.textContent = 'Зашифрованное вложение VKEncrypt';
        }

        restoreMediaLink(link);

        delete box.dataset.vkP2PDecoded;
        delete box.dataset.vkP2PPlatformBlocked;
        if (!preserveAutoTried) {
            delete box.dataset.vkP2PAutoTried;
        }
    }

    function restoreAllIncomingMedia() {
        document.querySelectorAll('.vk-p2p-media-box').forEach(box => resetMediaInterface(box));
    }

    async function fetchEncryptedMediaBytes(link) {
        const url = link.href;

        if (typeof GM_xmlhttpRequest === 'function' && /^https?:/i.test(url) && !url.startsWith(location.origin)) {
            return new Promise((resolve, reject) => {
                GM_xmlhttpRequest({
                    method: 'GET',
                    url,
                    responseType: 'arraybuffer',
                    onload: (response) => {
                        if (response.status >= 200 && response.status < 300 && response.response) {
                            resolve(new Uint8Array(response.response));
                            return;
                        }

                        reject(new Error(`HTTP ${response.status}`));
                    },
                    onerror: () => {
                        reject(new Error('GM_xmlhttpRequest failed'));
                    }
                });
            });
        }

        const platformBlockReason = getCrossOriginMediaBlockReason(url);
        if (platformBlockReason) {
            throw new Error(platformBlockReason);
        }

        const response = await fetch(url, {
            credentials: 'include'
        });

        if (!response.ok) {
            throw new Error(`HTTP ${response.status}`);
        }

        return new Uint8Array(await response.arrayBuffer());
    }

    function renderDecryptedMedia(box, metadata, bytes, cachedObjectUrl = '') {
        const link = box.previousElementSibling?.matches?.('a[href]')
            ? box.previousElementSibling
            : null;
        const preview = box.querySelector('.vk-p2p-media-preview');
        const meta = box.querySelector('.vk-p2p-media-meta');
        const downloadLink = box.querySelector('.vk-p2p-media-download');
        const decryptBtn = box.querySelector('.vk-p2p-media-btn');
        const blob = new Blob([bytes], {
            type: metadata.mime || 'application/octet-stream'
        });
        const objectUrl = cachedObjectUrl || URL.createObjectURL(blob);

        if (preview?.dataset.vkP2PObjectUrl) {
            URL.revokeObjectURL(preview.dataset.vkP2PObjectUrl);
        }

        preview.innerHTML = '';
        preview.dataset.vkP2PObjectUrl = objectUrl;

        if (/^image\//i.test(metadata.mime || '')) {
            const img = document.createElement('img');
            img.src = objectUrl;
            img.alt = metadata.originalName || 'encrypted image';
            preview.appendChild(img);
        } else if (/^audio\//i.test(metadata.mime || '')) {
            const audio = document.createElement('audio');
            audio.controls = true;
            audio.src = objectUrl;
            preview.appendChild(audio);
        } else if (/^video\//i.test(metadata.mime || '')) {
            const video = document.createElement('video');
            video.controls = true;
            video.src = objectUrl;
            preview.appendChild(video);
        }

        if (meta) {
            meta.textContent = `${metadata.originalName || 'media'} • ${formatByteSize(metadata.originalSize || bytes.length)}`;
        }

        if (downloadLink) {
            downloadLink.href = objectUrl;
            downloadLink.download = metadata.originalName || 'media.bin';
            downloadLink.hidden = false;
        }

        if (link) {
            link.href = objectUrl;
            link.download = metadata.originalName || 'media.bin';
            setMediaLinkName(link, metadata.originalName || 'media.bin');
            setMediaLinkFootnote(link, metadata, metadata.originalSize || bytes.length);
        }

        if (decryptBtn) {
            decryptBtn.disabled = false;
            decryptBtn.hidden = true;
        }

        box.dataset.vkP2PDecoded = 'true';
    }

    async function decryptIncomingMediaLink(link, { manual = false } = {}) {
        const box = ensureMediaInterface(link);
        const decryptBtn = box.querySelector('.vk-p2p-media-btn');
        const error = box.querySelector('.vk-p2p-media-error');

        if (!hasAnyKeys()) {
            if (manual) showSeedSetupModal();
            return;
        }

        if (decryptBtn) {
            decryptBtn.disabled = true;
            decryptBtn.textContent = 'Расшифровываю...';
        }
        if (error) error.textContent = '';

        const platformBlockReason = getCrossOriginMediaBlockReason(link.href);
        if (platformBlockReason) {
            resetMediaInterface(box, { preserveAutoTried: !manual });
            applyMediaPlatformBlock(box, platformBlockReason);
            if (manual) {
                showToast(`⚠️ ${platformBlockReason}`);
            }
            return;
        }

        try {
            const cached = getCachedDecryptedMedia(link);
            if (cached) {
                renderDecryptedMedia(box, cached.metadata || {}, cached.bytes, cached.objectUrl || '');
            } else {
                const containerBytes = await fetchEncryptedMediaBytes(link);
                const parsed = parseEncryptedMediaContainer(containerBytes);
                const slotId = parsed.metadata?.keyId || currentKeySlot;
                const keyHex = getAllKeys()[slotId];

                if (!keyHex) {
                    throw new Error(`Ключ "${slotId}" не найден`);
                }

                const bytes = await decryptBinaryAESGCM(parsed.encryptedPayload, keyHex);
                const objectUrl = URL.createObjectURL(new Blob([bytes], {
                    type: parsed.metadata?.mime || 'application/octet-stream'
                }));

                setCachedDecryptedMedia(link, {
                    metadata: parsed.metadata || {},
                    bytes,
                    objectUrl
                });
                renderDecryptedMedia(box, parsed.metadata || {}, bytes, objectUrl);
            }
        } catch (err) {
            resetMediaInterface(box, { preserveAutoTried: !manual });
            if (error) {
                error.textContent = `ошибка: ${err.message}`;
            }
            if (manual) {
                showToast(`❌ Не удалось расшифровать вложение: ${err.message}`);
            }
        } finally {
            if (decryptBtn && box.dataset.vkP2PDecoded !== 'true') {
                decryptBtn.disabled = false;
            }
        }
    }

    function decorateIncomingMediaLinks() {
        getEncryptedMediaLinks().forEach(link => {
            const box = ensureMediaInterface(link);

            if (!settings.autoDecrypt) {
                if (!box.dataset.vkP2PDecoded) {
                    const btn = box.querySelector('.vk-p2p-media-btn');
                    if (btn) {
                        btn.hidden = false;
                        btn.textContent = '🔓 Расшифровать вложение';
                    }
                }
                return;
            }

            if (box.dataset.vkP2PAutoTried === 'true') return;
            observeMediaPreview(link, box);
        });
    }

    function observeMediaPreview(link, box) {
        if (!mediaPreviewObserver) {
            mediaPreviewObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (!entry.isIntersecting) return;

                    const targetBox = entry.target;
                    mediaPreviewObserver.unobserve(targetBox);

                    if (targetBox.dataset.vkP2PAutoTried === 'true') return;
                    targetBox.dataset.vkP2PAutoTried = 'true';

                    const targetLink = targetBox.previousElementSibling?.matches?.('a[href]')
                        ? targetBox.previousElementSibling
                        : null;
                    if (!targetLink) return;

                    decryptIncomingMediaLink(targetLink, { manual: false });
                });
            }, {
                root: null,
                rootMargin: '120px 0px',
                threshold: 0.01
            });
        }

        mediaPreviewObserver.observe(box);
    }

    function restoreIncomingMessage(msgEl) {
        const originalEnc = msgEl.dataset.vkdecOriginal;
        if (!originalEnc) return;

        msgEl.innerHTML = '';
        msgEl.textContent = originalEnc;
        delete msgEl.dataset.vkdecDone;
    }

    function restoreAllIncomingMessages() {
        document.querySelectorAll('[data-vkdec-original]').forEach(el => restoreIncomingMessage(el));
    }

    function extractNodeText(node) {
        if (!node) return '';

        if (node.nodeType === Node.TEXT_NODE) {
            return node.textContent || '';
        }

        if (node.nodeType !== Node.ELEMENT_NODE) {
            return '';
        }

        const el = /** @type {HTMLElement} */ (node);

        if (el.dataset?.vkdecSkip === 'true') {
            return '';
        }

        if (el.tagName === 'IMG') {
            return el.getAttribute('alt') || '';
        }

        let out = '';
        el.childNodes.forEach(child => {
            out += extractNodeText(child);
        });
        return out;
    }

    function extractMessageText(msgEl) {
        return extractNodeText(msgEl).trim();
    }

    async function processIncomingMessage(msgEl) {
        if (!settings.autoDecrypt) return;
        if (!hasAnyKeys()) return;
        if (msgEl.dataset.vkdecDone) return;

        const text = extractMessageText(msgEl);
        const parsed = parseEncryptedMessage(text);
        if (parsed) {
            const keyHex = getAllKeys()[parsed.keyId];
            if (!keyHex) return;

            try {
                const payload = decodePayloadForCodec(parsed.encodedPayload, parsed.codecId);
                const decrypted = await decryptAESGCM(payload, keyHex);
                createToggleInterface(parsed.originalText, decrypted, msgEl);
            } catch (err) {
                console.error('❌ Ошибка расшифровки:', err);
                createErrorInterface(parsed.originalText, err.message, msgEl);
            }
            return;
        }

        if (!isWordsDictionaryText(text)) return;

        for (const keyHex of Object.values(getAllKeys())) {
            try {
                const packet = await decryptWordMessage(text, keyHex);
                if (packet) {
                    await acceptWordFragment(packet, msgEl, text);
                    return;
                }
            } catch {
                // Обычный текст из словаря должен оставаться обычным текстом.
            }
        }
    }

    function getIncomingMessageElements() {
        const elements = new Set();

        document.querySelectorAll(
            '.ConvoMessage__text, .MessageText, .im_msg_text, .im-message--text'
        ).forEach(el => {
            if (el.closest('[data-vkdec-done="true"]')) return;
            elements.add(el);
        });

        document.querySelectorAll('[role="list"][aria-label*="Сообщения"]').forEach(list => {
            list.querySelectorAll('article span, article div').forEach(el => {
                if (el.dataset.vkdecSkip === 'true') return;
                if (el.closest('[data-vkdec-done="true"]')) return;
                if (el.children.length) return;

                const text = extractMessageText(el);
                if (parseEncryptedMessage(text) || isWordsDictionaryText(text)) {
                    elements.add(el);
                }
            });
        });

        return elements;
    }
    // ============================================================
    // Composer helpers
    // ============================================================

    function getComposerInput() {
        const selectors = [
            '.ComposerInput__input.ConvoComposer__input[contenteditable="true"]',
            '.ConvoComposer__input[contenteditable="true"]',
            '.im-editable[contenteditable="true"]',
            '[contenteditable="true"][role="textbox"]',
            '[contenteditable="true"]'
        ];

        for (const selector of selectors) {
            const list = Array.from(document.querySelectorAll(selector));
            const visible = list.find(el => {
                const rect = el.getBoundingClientRect();
                return rect.width > 20 && rect.height > 10;
            });

            if (visible) return visible;
        }

        return null;
    }

    function getComposerPanel(inputEl) {
        if (!inputEl) return null;

        const knownPanel = inputEl.closest(
            '.ConvoComposer__inputPanel, .ConvoComposer, .im-compose, .im-chat-input, form'
        );

        if (knownPanel) return knownPanel;

        let node = inputEl.parentElement;

        while (node && node !== document.body) {
            const rect = node.getBoundingClientRect();
            const hasComposerButtons = Boolean(node.querySelector(
                'button, [role="button"], [aria-label*="Загрузить файл"], [aria-label*="эмодзи"], [aria-label*="голосового"]'
            ));

            if (hasComposerButtons && rect.width > 80 && rect.height > 20) {
                return node;
            }

            node = node.parentElement;
        }

        return inputEl.parentElement;
    }

    function findSendButton(panel) {
        const root = panel || document;

        const selectors = [
            '.ConvoComposer__buttonIcon--submit',
            'button[aria-label*="Отправить"]',
            '[aria-label*="Отправить"]',
            'button[type="submit"]',
            '.im-send-btn',
            '.ConvoComposer__button:last-child'
        ];

        for (const selector of selectors) {
            const found = root.querySelector(selector);
            if (!found) continue;

            const button = found.closest('button, [role="button"], a, div') || found;
            const rect = button.getBoundingClientRect();

            if (rect.width > 0 && rect.height > 0) {
                return button;
            }
        }

        const icon = root.querySelector('svg.vkuiIcon--send_24, .vkuiIcon--send_24');
        if (icon) {
            return icon.closest('button, [role="button"], a, div') || icon;
        }

        return null;
    }

    function findComposerButton(panel, labels) {
        const root = panel || document;

        for (const label of labels) {
            const selector = [
                `button[aria-label*="${label}"]`,
                `[role="button"][aria-label*="${label}"]`,
                `[aria-label*="${label}"]`
            ].join(',');

            const found = root.querySelector(selector);
            if (!found) continue;

            const button = found.closest('button, [role="button"], a, div') || found;
            const rect = button.getBoundingClientRect();

            if (rect.width > 0 && rect.height > 0) {
                return button;
            }
        }

        return null;
    }

    function findUploadButton(panel) {
        return findComposerButton(panel, [
            'Загрузить файл',
            'Прикрепить',
            'прикрепить',
            'файл'
        ]);
    }

    function findEmojiButton(panel) {
        return findComposerButton(panel, [
            'эмодзи',
            'Эмодзи',
            'стикер',
            'Стикер'
        ]);
    }

    function getDirectChildWithin(parent, child) {
        if (!parent || !child || !parent.contains(child)) return null;

        let node = child;
        while (node.parentElement && node.parentElement !== parent) {
            node = node.parentElement;
        }

        return node.parentElement === parent ? node : null;
    }

    function insertAfterReference(node, reference) {
        if (!node || !reference?.parentNode) return false;
        reference.parentNode.insertBefore(node, reference.nextSibling);
        return true;
    }

    function insertBeforeReference(node, reference) {
        if (!node || !reference?.parentNode) return false;
        reference.parentNode.insertBefore(node, reference);
        return true;
    }

    function getComposerInsertReference(panel, inputEl) {
        if (!panel || !inputEl) return null;

        let node = inputEl;

        while (node.parentElement && node.parentElement !== panel) {
            node = node.parentElement;
        }

        return node.parentElement === panel ? node : inputEl;
    }

    function getInputPlainText(inputEl) {
        if (!inputEl) return '';

        if ('value' in inputEl) {
            return inputEl.value.trim();
        }

        return inputEl.innerText.trim();
    }

    function setInputPlainText(inputEl, text) {
        if (!inputEl) return;

        inputEl.focus();

        if ('value' in inputEl) {
            inputEl.value = text;
        } else {
            inputEl.innerText = text;

            const range = document.createRange();
            const sel = window.getSelection();

            range.selectNodeContents(inputEl);
            range.collapse(false);

            sel.removeAllRanges();
            sel.addRange(range);
        }

        inputEl.dispatchEvent(new InputEvent('input', {
            bubbles: true,
            inputType: 'insertText',
            data: text
        }));

        inputEl.dispatchEvent(new Event('change', { bubbles: true }));
    }

    function isComposerFileInput(inputEl) {
        if (!inputEl || inputEl.type !== 'file') return false;
        if (!getComposerInput()) return false;

        const accept = String(inputEl.accept || '').toLowerCase();
        return (
            accept.includes('image') ||
            accept.includes('audio') ||
            accept.includes('video') ||
            accept.includes('*/*') ||
            accept === ''
        );
    }

    async function handleMediaFileInputChange(event) {
        const inputEl = event.target;

        if (!(inputEl instanceof HTMLInputElement) || inputEl.type !== 'file') return;
        if (inputEl.dataset.vkP2PMediaSynthetic === 'true') {
            delete inputEl.dataset.vkP2PMediaSynthetic;
            return;
        }
        if (!settings.encryptMediaUploads) return;
        if (!isComposerFileInput(inputEl)) return;

        const files = Array.from(inputEl.files || []);
        if (!files.length) return;

        const hasMedia = files.some(file => isEncryptableMediaFile(file));
        if (!hasMedia) return;

        if (!hasAnyKeys()) {
            event.preventDefault();
            event.stopPropagation();
            event.stopImmediatePropagation?.();
            inputEl.value = '';
            showSeedSetupModal();
            return;
        }

        const keyHex = getCurrentKeyHex();
        if (!keyHex) {
            event.preventDefault();
            event.stopPropagation();
            event.stopImmediatePropagation?.();
            showToast(`❌ Ключ "${currentKeySlot}" не найден`);
            return;
        }

        event.preventDefault();
        event.stopPropagation();
        event.stopImmediatePropagation?.();

        try {
            const outputFiles = [];

            for (const file of files) {
                if (isEncryptableMediaFile(file)) {
                    outputFiles.push(await buildEncryptedMediaFile(file, keyHex, currentKeySlot));
                } else {
                    outputFiles.push(file);
                }
            }

            const dataTransfer = new DataTransfer();
            outputFiles.forEach(file => dataTransfer.items.add(file));
            inputEl.files = dataTransfer.files;
            inputEl.dataset.vkP2PMediaSynthetic = 'true';
            inputEl.dispatchEvent(new Event('change', { bubbles: true }));

            const mediaCount = outputFiles.filter(file => isEncryptedMediaName(file.name)).length;
            showToast(`✅ Зашифровал вложения до отправки: ${mediaCount}`);
        } catch (err) {
            inputEl.value = '';
            showToast(`❌ Не удалось зашифровать вложения: ${err.message}`);
        }
    }

    async function encryptCurrentInput({ showErrors = true } = {}) {
        if (!hasAnyKeys()) {
            showSeedSetupModal();
            return false;
        }

        const inputEl = getComposerInput();

        if (!inputEl) {
            if (showErrors) showToast('❌ Не нашёл поле ввода');
            return false;
        }

        const plainText = getInputPlainText(inputEl);

        if (!plainText) return false;

        if (parseEncryptedMessage(plainText)) {
            return true;
        }

        const keyHex = getCurrentKeyHex();

        if (!keyHex) {
            if (showErrors) showToast(`❌ Ключ "${currentKeySlot}" не найден`);
            return false;
        }

        try {
            const codecId = normalizeCodecId(settings.cipherCodec);

            if (codecId === 'words') {
                const messages = await encodePlaintextToWordMessages(plainText, keyHex);
                pendingWordMessages = messages.slice(1);
                setInputPlainText(inputEl, messages[0]);
                lastEncryptedAt = Date.now();
                if (messages.length > 1) {
                    showToast(`✅ Сообщение разбито на ${messages.length} частей`);
                }
                return true;
            }

            const b64 = await encryptAESGCM(plainText, keyHex);
            pendingWordMessages = [];
            const payload = encodePayloadForCodec(b64, codecId);
            const encryptedMsg = formatEncryptedMessage(currentKeySlot, payload, codecId);

            setInputPlainText(inputEl, encryptedMsg);
            lastEncryptedAt = Date.now();

            return true;
        } catch (err) {
            console.error('❌ Ошибка шифрования:', err);
            if (showErrors) showToast('❌ Не удалось зашифровать: ' + err.message);
            return false;
        }
    }

    async function sendPendingWordMessages() {
        while (pendingWordMessages.length) {
            const inputEl = getComposerInput();
            const panel = getComposerPanel(inputEl);
            const sendBtn = findSendButton(panel);
            if (!inputEl || !sendBtn) {
                showToast('⚠️ Не удалось отправить оставшиеся части');
                return;
            }

            setInputPlainText(inputEl, pendingWordMessages.shift());
            sendBtn.click();
            await new Promise(resolve => setTimeout(resolve, 220));
        }
    }

    async function autoEncryptAndSend(event) {
        if (skipNextAutoEncrypt) return;
        if (!settings.autoEncrypt) return;
        if (isAutoSending) return;

        if (!hasAnyKeys()) {
            if (event) {
                event.preventDefault();
                event.stopPropagation();
                event.stopImmediatePropagation?.();
            }
            showSeedSetupModal();
            return;
        }

        const inputEl = getComposerInput();
        if (!inputEl) return;

        const plainText = getInputPlainText(inputEl);
        if (!plainText) return;

        if (parseEncryptedMessage(plainText)) {
            return;
        }

        if (event) {
            event.preventDefault();
            event.stopPropagation();
            event.stopImmediatePropagation?.();
        }

        const ok = await encryptCurrentInput({ showErrors: true });
        if (!ok) return;

        isAutoSending = true;

        setTimeout(() => {
            try {
                const freshInput = getComposerInput();
                const panel = getComposerPanel(freshInput);
                const sendBtn = findSendButton(panel);

                if (sendBtn) {
                    sendBtn.click();
                    if (pendingWordMessages.length) {
                        setTimeout(() => {
                            sendPendingWordMessages();
                        }, 260);
                    }
                } else {
                    showToast('⚠️ Зашифровал, но не нашёл кнопку отправки');
                }
            } finally {
                setTimeout(() => {
                    isAutoSending = false;
                }, 300);
            }
        }, 120);
    }

    function handleComposerKeydown(e) {
        if (skipNextAutoEncrypt) return;
        if (!settings.autoEncrypt) return;
        if (e.key !== 'Enter') return;

        // Shift+Enter оставляем для переноса строки.
        if (e.shiftKey || e.ctrlKey || e.altKey || e.metaKey) return;

        autoEncryptAndSend(e);
    }

    function attachEnterHandler(inputEl) {
        if (!inputEl || inputEl.dataset.vkP2PEnterAttached === 'true') return;

        inputEl.dataset.vkP2PEnterAttached = 'true';
        inputEl.addEventListener('keydown', handleComposerKeydown, true);
    }

    function attachSendButtonHandler(sendBtn) {
        if (!sendBtn || sendBtn.dataset.vkP2PSendAttached === 'true') return;

        sendBtn.dataset.vkP2PSendAttached = 'true';

        sendBtn.addEventListener('click', (e) => {
            if (pendingWordMessages.length && !settings.autoEncrypt && !isAutoSending) {
                e.preventDefault();
                e.stopPropagation();
                e.stopImmediatePropagation?.();
                isAutoSending = true;
                sendBtn.click();
                setTimeout(() => {
                    sendPendingWordMessages().finally(() => {
                        isAutoSending = false;
                    });
                }, 260);
                return;
            }

            if (skipNextAutoEncrypt) return;
            if (!settings.autoEncrypt) return;
            if (isAutoSending) return;

            const now = Date.now();

            // Если только что зашифровали вручную, не мешаем отправке.
            if (now - lastEncryptedAt < 250) return;

            autoEncryptAndSend(e);
        }, true);
    }
    // ============================================================
    // Protected voice recording
    // ============================================================

    let activeVoiceRecording = null;
    let voiceRecorderClickInstalled = false;

    function getVoiceRecorderButton(target) {
        const button = target?.closest?.('button, [role="button"]');
        if (!button) return null;
        if (button.dataset.vkP2PVoiceButton === 'true') return button;

        const label = `${button.getAttribute('aria-label') || ''} ${button.title || ''}`;
        if (/(голосов(ое|ого|ую)|аудиосообщени|начать запись)/i.test(label)) return button;

        const isMicButton = button.matches(
            '.ConvoComposer__sendButton--mic, [class*="sendButton--mic"], [class*="send-button--mic"]'
        );
        if (!isMicButton) return null;

        return getInputPlainText(getComposerInput()) ? null : button;
    }

    function getVoiceRecorderMimeType() {
        const candidates = RUNTIME_PLATFORM.isSafari
            ? ['audio/mp4', 'audio/webm;codecs=opus', 'audio/webm', 'audio/ogg;codecs=opus']
            : ['audio/webm;codecs=opus', 'audio/ogg;codecs=opus', 'audio/webm', 'audio/mp4'];

        if (typeof MediaRecorder?.isTypeSupported !== 'function') {
            return '';
        }

        return candidates.find(type => MediaRecorder.isTypeSupported(type)) || '';
    }

    function getVoiceFileExtension(mimeType) {
        const mime = String(mimeType || '').toLowerCase();
        if (mime.includes('ogg')) return 'ogg';
        if (mime.includes('mp4') || mime.includes('aac')) return 'm4a';
        if (mime.includes('wav')) return 'wav';
        return 'webm';
    }

    function stopVoiceStream(stream) {
        stream?.getTracks?.().forEach(track => track.stop());
    }

    function findVoiceAttachmentInput(panel) {
        const inputs = Array.from(document.querySelectorAll('input[type="file"]'))
            .filter(input => !input.disabled && input.isConnected);
        if (!inputs.length) return null;

        const composerRoot = panel?.closest?.('.ConvoComposer, form, .im-compose, .im-chat-input') || panel;
        const score = input => {
            const accept = String(input.accept || '').toLowerCase();
            const hint = `${input.name || ''} ${input.id || ''} ${input.className || ''}`.toLowerCase();
            let value = 0;

            if (panel?.contains(input)) value += 20;
            if (composerRoot?.contains(input)) value += 10;
            if (!accept || accept.includes('*/*')) value += 8;
            if (accept.includes('application') || accept.includes('.vke')) value += 6;
            if (accept.includes('audio')) value += 3;
            if (/file|doc|attach|upload/.test(hint)) value += 4;
            if (accept.includes('image') && !accept.includes('audio') && !accept.includes('*')) value -= 8;

            return value;
        };

        return inputs.sort((left, right) => score(right) - score(left))[0] || null;
    }

    function attachEncryptedVoiceFile(panel, file) {
        const input = findVoiceAttachmentInput(panel);
        if (!input) {
            throw new Error('Не найден input вложений VK. Откройте меню «+» и повторите запись');
        }
        if (typeof DataTransfer !== 'function') {
            throw new Error('Платформа не поддерживает прикрепление записанного файла');
        }

        const dataTransfer = new DataTransfer();
        dataTransfer.items.add(file);
        input.files = dataTransfer.files;
        input.dataset.vkP2PMediaSynthetic = 'true';
        input.dispatchEvent(new Event('change', { bubbles: true }));
    }

    function createVoiceRecordingStatus(session) {
        const status = document.createElement('span');
        status.className = 'vk-p2p-voice-status';
        status.setAttribute('role', 'status');
        status.dataset.vkdecSkip = 'true';

        const reference = getDirectChildWithin(session.panel, session.button) || session.button;
        if (reference?.parentNode) {
            reference.parentNode.insertBefore(status, reference);
        } else {
            session.panel?.appendChild(status);
        }

        session.status = status;
        updateVoiceRecordingStatus(session);
    }

    function updateVoiceRecordingStatus(session) {
        if (!session.status) return;

        if (session.state === 'requesting') {
            session.status.textContent = 'Микрофон…';
            return;
        }
        if (session.state === 'encrypting') {
            session.status.textContent = 'Шифрую…';
            return;
        }

        const elapsed = Math.max(0, Math.floor((Date.now() - session.startedAt) / 1000));
        const minutes = String(Math.floor(elapsed / 60)).padStart(2, '0');
        const seconds = String(elapsed % 60).padStart(2, '0');
        session.status.textContent = `● ${minutes}:${seconds}`;
    }

    function markVoiceButtonRecording(session) {
        const button = session.button;
        session.originalAriaLabel = button.getAttribute('aria-label');
        session.originalTitle = button.getAttribute('title');
        button.dataset.vkP2PVoiceButton = 'true';
        button.classList.add('vk-p2p-voice-recording');
        button.setAttribute('aria-pressed', 'true');
        button.setAttribute('aria-label', 'Остановить и зашифровать голосовое сообщение');
        button.title = 'Остановить и прикрепить зашифрованную запись';
    }

    function restoreVoiceButton(session) {
        const button = session?.button;
        if (!button) return;

        button.classList.remove('vk-p2p-voice-recording');
        button.removeAttribute('aria-pressed');
        delete button.dataset.vkP2PVoiceButton;

        if (session.originalAriaLabel === null) button.removeAttribute('aria-label');
        else button.setAttribute('aria-label', session.originalAriaLabel);

        if (session.originalTitle === null) button.removeAttribute('title');
        else button.setAttribute('title', session.originalTitle);
    }

    function cleanupVoiceRecording(session) {
        if (!session) return;
        clearInterval(session.timer);
        stopVoiceStream(session.stream);
        session.status?.remove();
        restoreVoiceButton(session);
        if (activeVoiceRecording === session) activeVoiceRecording = null;
    }

    async function finishVoiceRecording(session) {
        if (session.finished) return;
        session.finished = true;

        if (session.cancelled) {
            cleanupVoiceRecording(session);
            return;
        }

        session.state = 'encrypting';
        updateVoiceRecordingStatus(session);

        try {
            const mimeType = session.recorder.mimeType || session.mimeType || 'audio/webm';
            const recording = new Blob(session.chunks, { type: mimeType });
            if (!recording.size) throw new Error('Запись получилась пустой');

            const extension = getVoiceFileExtension(mimeType);
            const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
            const sourceFile = new File(
                [recording],
                `voice-${timestamp}.${extension}`,
                { type: mimeType, lastModified: Date.now() }
            );
            const encryptedFile = await buildEncryptedMediaFile(
                sourceFile,
                session.keyHex,
                session.keySlot
            );

            attachEncryptedVoiceFile(session.panel, encryptedFile);
            showToast('✅ Голосовое зашифровано и прикреплено');
        } catch (err) {
            console.error('❌ Ошибка защищённой голосовой записи:', err);
            showToast(`❌ Не удалось прикрепить голосовое: ${err.message}`);
        } finally {
            cleanupVoiceRecording(session);
        }
    }

    async function startProtectedVoiceRecording(button) {
        if (!hasAnyKeys()) {
            showSeedSetupModal();
            return;
        }
        if (!navigator.mediaDevices?.getUserMedia || typeof MediaRecorder !== 'function') {
            showToast(`❌ ${getPlatformDisplayName()} не поддерживает запись через MediaRecorder`);
            return;
        }

        const keyHex = getCurrentKeyHex();
        if (!keyHex) {
            showToast(`❌ Ключ "${currentKeySlot}" не найден`);
            return;
        }

        const input = getComposerInput();
        const panel = getComposerPanel(input) || button.closest('form') || button.parentElement;
        const session = {
            button,
            panel,
            keyHex,
            keySlot: currentKeySlot,
            chunks: [],
            state: 'requesting',
            startedAt: Date.now(),
            finished: false,
            cancelled: false,
            stream: null,
            recorder: null,
            status: null,
            timer: null,
        };
        activeVoiceRecording = session;
        markVoiceButtonRecording(session);
        createVoiceRecordingStatus(session);

        try {
            const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
            if (session.cancelled || activeVoiceRecording !== session) {
                stopVoiceStream(stream);
                return;
            }

            const mimeType = getVoiceRecorderMimeType();
            const recorder = mimeType
                ? new MediaRecorder(stream, { mimeType })
                : new MediaRecorder(stream);
            session.stream = stream;
            session.recorder = recorder;
            session.mimeType = mimeType;
            session.state = 'recording';
            session.startedAt = Date.now();

            recorder.addEventListener('dataavailable', event => {
                if (event.data?.size) session.chunks.push(event.data);
            });
            recorder.addEventListener('stop', () => finishVoiceRecording(session), { once: true });
            recorder.addEventListener('error', event => {
                const message = event.error?.message || 'MediaRecorder error';
                showToast(`❌ Ошибка записи: ${message}`);
                cleanupVoiceRecording(session);
            }, { once: true });
            recorder.start(250);
            session.timer = setInterval(() => updateVoiceRecordingStatus(session), 1000);
            updateVoiceRecordingStatus(session);
        } catch (err) {
            cleanupVoiceRecording(session);
            showToast(`❌ Не удалось начать запись: ${err.message}`);
        }
    }

    function stopProtectedVoiceRecording() {
        const session = activeVoiceRecording;
        if (!session) return;

        if (session.state === 'requesting') {
            session.cancelled = true;
            cleanupVoiceRecording(session);
            return;
        }
        if (session.state !== 'recording') return;

        session.state = 'encrypting';
        updateVoiceRecordingStatus(session);
        session.recorder.stop();
        stopVoiceStream(session.stream);
    }

    function cancelProtectedVoiceRecording() {
        const session = activeVoiceRecording;
        if (!session) return;

        session.cancelled = true;
        if (session.recorder?.state === 'recording') {
            session.recorder.onstop = null;
            session.recorder.stop();
        }
        cleanupVoiceRecording(session);
        showToast('⏸️ Голосовая запись отменена');
    }

    function handleProtectedVoiceClick(event) {
        const button = getVoiceRecorderButton(event.target);
        if (!button || !settings.encryptMediaUploads) return;

        event.preventDefault();
        event.stopPropagation();
        event.stopImmediatePropagation?.();

        if (activeVoiceRecording) {
            stopProtectedVoiceRecording();
            return;
        }

        startProtectedVoiceRecording(button);
    }

    function initProtectedVoiceRecorder() {
        if (voiceRecorderClickInstalled) return;
        voiceRecorderClickInstalled = true;
        document.addEventListener('click', handleProtectedVoiceClick, true);
    }

    function maintainProtectedVoiceRecorder() {
        if (activeVoiceRecording && !activeVoiceRecording.button.isConnected) {
            cancelProtectedVoiceRecording();
        }
    }
    // ============================================================
    // Composer controls
    // ============================================================

    function setIconButtonGlyph(button, glyph) {
        if (!button) return;

        let glyphEl = button.querySelector('.vk-p2p-icon-glyph');
        if (!glyphEl) {
            glyphEl = document.createElement('span');
            glyphEl.className = 'vk-p2p-icon-glyph';
            button.replaceChildren(glyphEl);
        }

        glyphEl.textContent = glyph;
    }

    function updateEncryptButtonsTitle() {
        const encBtn = document.getElementById('vk-p2p-enc-btn');
        const keyBtn = document.getElementById('vk-p2p-key-btn');
        const encWrapper = document.getElementById('vk-p2p-enc-controls');
        const hasKeys = hasAnyKeys();
        const shouldHideEncryptButton = settings.autoEncrypt && hasKeys;

        if (encBtn) {
            encBtn.title = hasKeys
                ? `Зашифровать сообщение. Ключ: ${currentKeySlot}`
                : 'Настроить ключи VKEncrypt';

            setIconButtonGlyph(encBtn, hasKeys ? '🔒' : '🔐');
            encBtn.style.opacity = hasKeys ? '0.58' : '0.35';
            encBtn.style.display = shouldHideEncryptButton ? 'none' : '';
        }

        if (encWrapper) {
            encWrapper.style.display = shouldHideEncryptButton ? 'none' : '';
        }

        if (keyBtn) {
            keyBtn.title = hasKeys
                ? `Настройки VKEncrypt. Сейчас: ${currentKeySlot}`
                : 'Настроить VKEncrypt';

            setIconButtonGlyph(keyBtn, !hasKeys
                ? '⚙️'
                : currentKeySlot === '@temp'
                    ? '⚡'
                    : settings.autoEncrypt
                        ? '🟢'
                        : '🔑');
        }
    }

    function addEncryptButton() {
        const inputEl = getComposerInput();
        if (!inputEl) return;

        attachEnterHandler(inputEl);

        const panel = getComposerPanel(inputEl);
        if (!panel) return;

        const sendBtn = findSendButton(panel);
        if (sendBtn) attachSendButtonHandler(sendBtn);

        if (document.getElementById('vk-p2p-enc-controls') && document.getElementById('vk-p2p-key-controls')) {
            updateEncryptButtonsTitle();
            return;
        }

        const keyWrapper = document.createElement('span');
        keyWrapper.id = 'vk-p2p-key-controls';
        keyWrapper.className = 'vk-p2p-controls';

        const encWrapper = document.createElement('span');
        encWrapper.id = 'vk-p2p-enc-controls';
        encWrapper.className = 'vk-p2p-controls';

        const encBtn = document.createElement('button');
        encBtn.id = 'vk-p2p-enc-btn';
        encBtn.className = 'vk-p2p-icon-btn vk-p2p-icon-btn-main';
        encBtn.type = 'button';
        setIconButtonGlyph(encBtn, '🔒');

        const keyBtn = document.createElement('button');
        keyBtn.id = 'vk-p2p-key-btn';
        keyBtn.className = 'vk-p2p-icon-btn vk-p2p-icon-btn-small';
        keyBtn.type = 'button';

        encBtn.addEventListener('click', async (e) => {
            e.preventDefault();
            e.stopPropagation();

            if (!hasAnyKeys()) {
                showSeedSetupModal();
                return;
            }

            encBtn.disabled = true;
            encBtn.textContent = '⏳';

            try {
                const ok = await encryptCurrentInput({ showErrors: true });
                if (ok) showToast('✅ Сообщение зашифровано');
            } finally {
                encBtn.disabled = false;
                setIconButtonGlyph(encBtn, '🔒');
                updateEncryptButtonsTitle();
            }
        });

        keyBtn.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();

            if (!hasAnyKeys()) {
                showSeedSetupModal();
                return;
            }

            showMainMenu(keyBtn);
        });

        keyWrapper.appendChild(keyBtn);
        encWrapper.appendChild(encBtn);

        const uploadBtn = findUploadButton(panel);
        const emojiBtn = findEmojiButton(panel);
        const uploadReference = getDirectChildWithin(panel, uploadBtn) || uploadBtn;
        const emojiReference = getDirectChildWithin(panel, emojiBtn) || emojiBtn;
        const sendContainer = sendBtn?.closest('.DropdownReforged');
        const sendReference = getDirectChildWithin(panel, sendContainer || sendBtn) || sendContainer || sendBtn;
        const insertReference = getComposerInsertReference(panel, inputEl);

        if (uploadReference && panel.contains(uploadReference)) {
            insertAfterReference(keyWrapper, uploadReference);
        } else if (insertReference?.parentNode) {
            insertReference.parentNode.insertBefore(keyWrapper, insertReference);
        } else {
            panel.insertBefore(keyWrapper, panel.firstChild);
        }

        if (emojiReference && panel.contains(emojiReference)) {
            insertBeforeReference(encWrapper, emojiReference);
        } else if (sendReference && panel.contains(sendReference)) {
            insertBeforeReference(encWrapper, sendReference);
        } else if (insertReference?.parentNode) {
            insertAfterReference(encWrapper, insertReference);
        } else {
            panel.appendChild(encWrapper);
        }

        updateEncryptButtonsTitle();
    }

    function buildShareInstructionText(options = {}) {
        const includeInstallUrl = options.includeInstallUrl !== false;
        const includeCyberChef = options.includeCyberChef !== false;
        const includeNoteServices = options.includeNoteServices !== false;
        const lines = [
            'Я шифрую сообщения через VKEncrypt, чтобы переписка не лежала открытым текстом в VK/MAX/OK и обычных чатах.',
            'Чтобы читать мои сообщения, установи VKEncrypt и введи такой же секретный ключ/seed.'
        ];

        if (includeInstallUrl) {
            lines.push(`Инструкция установки: ${README_URL}`);
            lines.push(`Быстрая ссылка на userscript: ${INSTALL_URL}`);
        }

        if (includeCyberChef) {
            lines.push(`Запасной ручной инструмент для проверки кодировок: CyberChef ${CYBERCHEF_URL}`);
        }

        lines.push('Ключ я отправлю отдельно через защищённый канал связи.');
        lines.push('И мне, и тебе нельзя отправлять ключ в VK, MAX, OK, обычном Telegram-чате, email, SMS или обычной почтой.');

        if (includeNoteServices) {
            lines.push(`Для одноразовой передачи ключа можно использовать: ${ONE_TIME_NOTE_SERVICES.join(' ; ')}`);
        }

        return lines.join('\n');
    }

    function sendPlainTextMessage(text, { sendNow = false } = {}) {
        const inputEl = getComposerInput();

        if (!inputEl) {
            showToast('❌ Не нашёл поле ввода');
            return false;
        }

        setInputPlainText(inputEl, text);

        if (!sendNow) {
            showToast('✅ Инструкция вставлена в поле ввода');
            return true;
        }

        const panel = getComposerPanel(inputEl);
        const sendBtn = findSendButton(panel);

        if (!sendBtn) {
            showToast('⚠️ Инструкция вставлена, но кнопку отправки не нашёл');
            return false;
        }

        skipNextAutoEncrypt = true;
        try {
            sendBtn.click();
            showToast('✅ Инструкция отправлена без шифрования');
            return true;
        } finally {
            setTimeout(() => {
                skipNextAutoEncrypt = false;
            }, 500);
        }
    }

    function showShareInstructionModal() {
        const { overlay, modal } = createModal({
            title: '📨 Скинуть инструкцию',
            bodyHtml: `
                <p>
                    Сообщение будет отправлено открытым текстом, чтобы собеседник смог установить VKEncrypt.
                    Ключ в это сообщение не добавляется.
                </p>

                <label class="vk-p2p-check">
                    <input id="vk-p2p-share-install-url" type="checkbox" checked>
                    <span>Добавить ссылку установки VKEncrypt</span>
                </label>

                <label class="vk-p2p-check">
                    <input id="vk-p2p-share-cyberchef" type="checkbox" checked>
                    <span>Добавить CyberChef как запасной ручной инструмент</span>
                </label>

                <label class="vk-p2p-check">
                    <input id="vk-p2p-share-note-services" type="checkbox" checked>
                    <span>Добавить сервисы для одноразовой передачи ключа</span>
                </label>

                <p class="vk-p2p-note">
                    Автосоздание одноразовой заметки пока выключено: публичные сервисы отличаются API/CORS.
                    Ключ лучше отправлять отдельно и только после проверки сервиса.
                </p>
            `,
            actionsHtml: `
                <button class="vk-p2p-btn vk-p2p-btn-secondary" id="vk-p2p-share-cancel">Отмена</button>
                <button class="vk-p2p-btn vk-p2p-btn-secondary" id="vk-p2p-share-insert">Вставить</button>
                <button class="vk-p2p-btn vk-p2p-btn-primary" id="vk-p2p-share-send">Вставить и отправить</button>
            `
        });

        function getText() {
            return buildShareInstructionText({
                includeInstallUrl: modal.querySelector('#vk-p2p-share-install-url').checked,
                includeCyberChef: modal.querySelector('#vk-p2p-share-cyberchef').checked,
                includeNoteServices: modal.querySelector('#vk-p2p-share-note-services').checked
            });
        }

        modal.querySelector('#vk-p2p-share-cancel').addEventListener('click', () => overlay.remove());
        modal.querySelector('#vk-p2p-share-insert').addEventListener('click', () => {
            const text = getText();
            overlay.remove();
            sendPlainTextMessage(text, { sendNow: false });
        });
        modal.querySelector('#vk-p2p-share-send').addEventListener('click', () => {
            const text = getText();
            overlay.remove();
            sendPlainTextMessage(text, { sendNow: true });
        });
    }

    function showMainMenu(anchorBtn) {
        closeMenus();

        const menu = document.createElement('div');
        menu.className = 'vk-p2p-menu';
        menu.style.left = '8px';
        menu.style.top = '8px';
        menu.style.visibility = 'hidden';

        const title = document.createElement('div');
        title.className = 'vk-p2p-menu-title';
        title.textContent = `${APP_NAME} v${APP_VERSION}`;
        menu.appendChild(title);

        const allKeys = getAllKeys();
        const keyNames = Object.keys(allKeys);

        if (keyNames.length) {
            const keyTitle = document.createElement('div');
            keyTitle.className = 'vk-p2p-menu-title';
            keyTitle.textContent = 'Ключ шифрования';
            menu.appendChild(keyTitle);

            keyNames.forEach(slotId => {
                const item = document.createElement('button');
                item.type = 'button';
                item.className = 'vk-p2p-menu-item';

                if (slotId === currentKeySlot) {
                    item.classList.add('vk-p2p-menu-item-active');
                }

                item.textContent = formatKeyDisplay(slotId);
                item.title = slotId === '@temp'
                    ? 'Временный ключ (только в памяти)'
                    : getCustomKeyLabel(slotId)
                        ? `${slotId} — ${getCustomKeyLabel(slotId)}`
                        : slotId;

                item.addEventListener('click', () => {
                    selectKeySlot(slotId, { rememberForChat: true });
                    closeMenus();
                    const chatId = getCurrentChatId();
                    showToast(chatId
                        ? `✅ Для этого чата выбран ключ: ${formatKeyDisplay(slotId)}`
                        : `✅ Выбран ключ: ${formatKeyDisplay(slotId)}`);
                });

                menu.appendChild(item);
            });
        }

        addMenuSeparator(menu);

        addMenuItem(menu, settings.autoEncrypt ? '🟢 Автошифрование: включено' : '⚪ Автошифрование: выключено', () => {
            settings.autoEncrypt = !settings.autoEncrypt;
            saveSettings();
            closeMenus();
            updateEncryptButtonsTitle();
            scan();
            showToast(settings.autoEncrypt ? '✅ Автошифрование включено' : '⏸️ Автошифрование выключено');
        });

        addMenuItem(menu, settings.encryptMediaUploads ? '🎙️ Шифровать вложения и голосовые: включено' : '🎙️ Шифровать вложения и голосовые: выключено', () => {
            settings.encryptMediaUploads = !settings.encryptMediaUploads;
            saveSettings();
            closeMenus();
            showToast(settings.encryptMediaUploads ? '✅ Шифрование вложений и голосовых включено' : '⏸️ Шифрование вложений и голосовых выключено');
        });

        addMenuSelect(
            menu,
            'Кодирование шифротекста',
            'vk-p2p-cipher-codec-select',
            normalizeCodecId(settings.cipherCodec),
            [
                { value: 'emoji', label: 'Emoji' },
                { value: 'cyrillic', label: 'Русский алфавит' },
                { value: 'words', label: 'Русские слова (экспериментально)' },
                { value: 'base64', label: 'Base64' }
            ],
            value => {
                settings.cipherCodec = normalizeCodecId(value);
                saveSettings();
                showToast(`✅ Новые сообщения будут в формате: ${getCipherCodecConfig(settings.cipherCodec).label}`);
            }
        );

        addMenuItem(menu, settings.autoDecrypt ? '👁️ Авто-расшифровка: включена' : '🙈 Авто-расшифровка: выключена', () => {
            settings.autoDecrypt = !settings.autoDecrypt;
            saveSettings();
            closeMenus();
            if (!settings.autoDecrypt) {
                restoreAllIncomingMessages();
                restoreAllIncomingMedia();
            }
            showToast(settings.autoDecrypt ? '✅ Авто-расшифровка включена' : '⏸️ Авто-расшифровка выключена');
            scan();
        });

        addMenuSeparator(menu);

        addMenuItem(menu, '➕ Добавить пользовательский ключ', () => {
            closeMenus();
            showAddCustomKeyModal();
        });

        addMenuItem(menu, '⚡ Сгенерировать временный ключ', () => {
            closeMenus();
            generateTempKey();
        });

        addMenuItem(menu, '🔄 Сменить seed-фразу k1–k4', () => {
            closeMenus();
            showSeedChangeModal();
        });

        addMenuItem(menu, '📨 Скинуть инструкцию по установке', () => {
            closeMenus();
            showShareInstructionModal();
        });

        if (TEMP_KEY) {
            addMenuItem(menu, '🧹 Удалить временный ключ', () => {
                TEMP_KEY = null;
                forgetChatKeySlot('@temp');
                if (currentKeySlot === '@temp') currentKeySlot = getFallbackKeySlot();
                closeMenus();
                updateEncryptButtonsTitle();
                showToast('✅ Временный ключ удалён');
            });
        }

        const customKeyNames = Object.keys(CUSTOM_KEYS);
        if (customKeyNames.length) {
            addMenuSeparator(menu);

            customKeyNames.forEach(name => {
                const label = getCustomKeyLabel(name);
                const display = label
                    ? `${name} (${truncateForDisplay(label)})`
                    : name;

                addMenuItem(menu, `🗑️ Удалить ключ ${display}`, () => {
                    if (!confirm(`Удалить пользовательский ключ "${name}"?`)) return;

                    delete CUSTOM_KEYS[name];
                    saveCustomKeys();
                    forgetChatKeySlot(name);

                    if (currentKeySlot === name) currentKeySlot = getFallbackKeySlot();

                    closeMenus();
                    updateEncryptButtonsTitle();
                    showToast(`✅ Ключ ${name} удалён`);
                }, true);
            });
        }

        addMenuSeparator(menu);

        addMenuItem(menu, '🧨 Сбросить все сохранённые ключи', () => {
            if (!confirm('Удалить сохранённые k1–k4 и все пользовательские ключи?')) return;

            closeMenus();
            resetAllKeys();
            showToast('✅ Сохранённые ключи сброшены');
        }, true);

        document.body.appendChild(menu);
        positionMenu(menu, anchorBtn);

        setTimeout(() => {
            document.addEventListener('click', function closeOnce(e) {
                if (!menu.contains(e.target) && e.target !== anchorBtn) {
                    menu.remove();
                }
            }, { once: true });
        }, 0);
    }

    function addMenuItem(menu, text, onClick, danger = false) {
        const item = document.createElement('button');
        item.type = 'button';
        item.className = 'vk-p2p-menu-item';

        if (danger) item.classList.add('vk-p2p-menu-danger');

        item.textContent = text;
        item.addEventListener('click', onClick);

        menu.appendChild(item);
        return item;
    }

    function addMenuSelect(menu, label, id, value, options, onChange) {
        const field = document.createElement('div');
        field.className = 'vk-p2p-menu-field';

        const labelEl = document.createElement('label');
        labelEl.className = 'vk-p2p-menu-label';
        labelEl.htmlFor = id;
        labelEl.textContent = label;

        const select = document.createElement('select');
        select.className = 'vk-p2p-menu-select';
        select.id = id;

        options.forEach(option => {
            const item = document.createElement('option');
            item.value = option.value;
            item.textContent = option.label;
            select.appendChild(item);
        });

        select.value = value;
        select.addEventListener('change', () => onChange(select.value));

        field.appendChild(labelEl);
        field.appendChild(select);
        menu.appendChild(field);
        return select;
    }

    function addMenuSeparator(menu) {
        const sep = document.createElement('div');
        sep.className = 'vk-p2p-menu-sep';
        menu.appendChild(sep);
    }

    function positionMenu(menu, anchorBtn) {
        const rect = anchorBtn.getBoundingClientRect();
        const margin = 8;
        const availableHeight = window.innerHeight - margin * 2;

        menu.style.maxHeight = `${availableHeight}px`;

        const menuRect = menu.getBoundingClientRect();
        const width = menuRect.width;
        const height = Math.min(menuRect.height, availableHeight);

        if (menuRect.height > availableHeight) {
            menu.style.height = `${availableHeight}px`;
        } else {
            menu.style.height = '';
        }
        const left = Math.max(margin, Math.min(rect.right - width, window.innerWidth - width - margin));
        const preferredTop = rect.top - height - margin;
        const fallbackTop = rect.bottom + margin;
        const top = preferredTop >= margin
            ? preferredTop
            : Math.min(fallbackTop, window.innerHeight - height - margin);

        menu.style.left = `${left}px`;
        menu.style.top = `${Math.max(margin, top)}px`;
        menu.style.visibility = 'visible';
    }
    // ============================================================
    // Scan loop
    // ============================================================

    function scan() {
        injectStyles();
        applyRememberedKeyForCurrentChat();

        getIncomingMessageElements().forEach(el => processIncomingMessage(el));
        decorateIncomingMediaLinks();

        addEncryptButton();
        maintainProtectedVoiceRecorder();
    }

    function scheduleScan(delay = 250) {
        if (scanTimer !== null) return;

        scanTimer = setTimeout(() => {
            scanTimer = null;
            scan();
        }, delay);
    }

    function init() {
        injectStyles();

        loadSettings();
        loadCustomKeys();
        loadChatKeySlots();

        DERIVED_KEYS = loadDerivedKeys();
        initProtectedVoiceRecorder();

        if (!DERIVED_KEYS && Object.keys(CUSTOM_KEYS).length) {
            currentKeySlot = Object.keys(CUSTOM_KEYS)[0];
        }

        applyRememberedKeyForCurrentChat({ force: true });
        initChatKeyNavigation();

        console.log(`🔐 ${APP_NAME} v${APP_VERSION} loaded`);
        console.log('🔑 Derived keys:', DERIVED_KEYS ? 'yes' : 'no');
        console.log('🔑 Custom keys:', Object.keys(CUSTOM_KEYS).join(', ') || 'none');
        console.log('⚡ Temp key:', TEMP_KEY ? 'yes' : 'no');

        scheduleScan(700);

        const observer = new MutationObserver(() => {
            scheduleScan();
        });

        observer.observe(document.body, {
            childList: true,
            subtree: true
        });

        document.addEventListener('change', (e) => {
            handleMediaFileInputChange(e);
        }, true);

        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                closeMenus();
                cancelProtectedVoiceRecording();
            }
        }, true);
    }

    init();

})();
